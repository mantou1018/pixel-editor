# 精灵图制作工具 V1 开发方案

## 1. 开发定位

本项目是 **做精灵图的工具**，不是泛泛的图片生成器。

V1 的核心主线是：

```text
创建 / 导入 / 像素化
-> SpriteDocument
-> Canvas 逐像素块编辑
-> 项目存档
-> JSON / PNG / sprite sheet 导出
```

AI 生成、导入现成精灵图、普通图片像素化都只是创建 `SpriteDocument` 的入口。编辑器内部只认识 `SpriteDocument`，不关心内容来自哪里。

## 2. 推荐技术栈

- `Next.js`：后续接 AI 接口方便，API Key 可以放后端。
- `React`：负责编辑器 UI、项目存档 UI、右侧面板和帧列表。
- `TypeScript`：约束 `SpriteDocument`、帧、像素和导出数据。
- `Canvas 2D`：负责中央像素画布，性能比 div 网格更稳。
- `IndexedDB`：负责本地项目存档，比 `localStorage` 更适合保存多个工程和缩略图。
- 前端图片处理：负责上传图片、采样、颜色归并、像素化。
- API Route：后续负责 AI 生成接口。

V1 不建议一开始引入复杂 UI 组件库。深色紫色像素风更适合自定义样式。

## 3. 核心数据模型

### 3.1 SpriteDocument

`SpriteDocument` 是一个精灵图工程文件。

建议字段：

```text
SpriteDocument
- id
- name
- schemaVersion
- createdAt
- updatedAt
- canvas
- palette
- layers
- frames
- animations
- sources
- exportSettings
- thumbnail
```

关系：

```text
SpriteDocument = 一个精灵图工程
Animation = 一个动作，例如 idle / walk / attack
Frame = 动作中的某一帧
Layer = 帧里的图层
PixelGrid = 可逐格编辑的像素数据
```

V1 可以只开放一个默认图层，但数据结构预留 `layers`，避免后续增加身体、头发、武器、阴影等图层时大返工。

### 3.2 像素数据

V1 推荐稀疏结构：

```text
"12,8": "#ffcc00"
"13,8": "#ffcc00"
```

透明像素不存。这样 JSON 更小，编辑和导出都更清楚。

## 4. 模块边界

建议按职责拆分：

```text
src/domain/sprite
```

只负责 `SpriteDocument` 类型、帧、图层、像素操作、调色板、动画规则。

```text
src/features/editor
```

负责编辑器界面和交互：Canvas、工具栏、右侧面板、帧列表、撤销重做。

```text
src/features/import
```

负责 AI 图、现成精灵图、普通图片转成 `SpriteDocument`。

```text
src/features/export
```

负责导出 JSON、PNG、sprite sheet。

```text
src/features/projects
```

负责项目存档、卡片网格、本地保存、重新打开。

```text
src/app/api/generate
```

后续只负责 AI 生成接口。AI 逻辑不能进入编辑器核心。

## 5. 开发阶段

### 阶段 0：项目骨架和规则确认

目标：建立技术底座，不急着做复杂功能。

建议创建：

- `src/domain/sprite/types.ts`
- `src/app/page.tsx`
- `src/features/editor/`
- `src/features/projects/`
- `src/features/import/`
- `src/features/export/`
- `src/lib/storage/`

需要确认：

- 项目采用 `Next.js + React + TypeScript`。
- 编辑区采用 `Canvas 2D`。
- 存档采用 `IndexedDB`。
- V1 支持 `16x16`、`32x32`、`64x64` 三档尺寸，默认推荐 `64x64`。
- V1 支持多个动作类型。
- 每个动作最多 12 帧。

### 阶段 1：首轮最小闭环

目标：不接 AI，只做一个真正可用的小编辑器。

必须完成：

1. 创建默认 `SpriteDocument`。
2. Canvas 显示像素格。
3. 点击单个像素块改颜色。
4. 保存到 IndexedDB。
5. 重新打开后能恢复。
6. 导出 JSON。
7. 导出 PNG。

建议模块：

- `createSpriteDocument`
- `SpriteCanvas`
- `EditorToolbar`
- `saveDocument`
- `loadDocument`
- `exportJson`
- `exportPng`

可以先用假数据：

- 默认示例精灵图。
- 简单项目列表。
- 简单缩略图或占位图。

质量门槛：

- 编辑一个像素不能影响相邻像素。
- 保存后刷新页面再打开，数据必须还原。
- JSON 和 PNG 导出必须反映编辑后的内容。

### 阶段 2：项目存档

目标：用户能管理多个精灵图项目，并且能可靠保存、重新打开和继续编辑。

必须完成：

- `ProjectSummary` 和 `SpriteDocument` 分离。
- 新建项目。
- 保存项目。
- 打开项目。
- 重命名项目。
- 删除项目，必须有二次确认。
- 项目存档卡片网格。
- 空状态。
- 卡片缩略图由真实 `SpriteDocument` 生成。

可以延后：

- 复制项目。
- 搜索项目。
- 项目分组。
- 自动备份历史版本。
- 自动保存。
- 云同步。

项目存档页只读取 `ProjectSummary`，不要直接加载完整 `SpriteDocument`。用户打开项目时，再读取完整文档。

存储采用 IndexedDB 本地项目库，不做云同步。第一轮只建两个对象仓库：

```text
project_summaries
project_documents
```

保存项目时必须同时更新：

```text
SpriteDocument：完整工程数据
ProjectSummary：卡片摘要、缩略图、更新时间、尺寸、状态
```

项目存档页的职责是项目管理入口，不直接编辑像素、帧或动作。

### 阶段 3：动作和帧管理

目标：支持多个动作类型，并让每个动作最多管理 12 帧。

必须完成：

- 动作列表。
- 当前动作高亮。
- 新增动作。
- 重命名动作。
- 删除动作，但至少保留 1 个动作。
- 帧列表。
- 当前帧高亮。
- 新增帧，每个动作最多 12 帧。
- 删除帧，但至少保留 1 帧。
- 复制当前帧。
- 简单播放 / 暂停。
- 帧时长。

暂不做：

- 多轨时间轴。
- 骨骼动画。
- 自动补间。
- 复杂关键帧。

### 阶段 4：图片像素化入口

目标：用户上传图片后，转换成 `SpriteDocument`，进入同一个编辑器。

必须完成：

- 上传图片。
- 按用户选择的目标尺寸采样，支持 `16x16`、`32x32`、`64x64`。
- 颜色归并，减少杂色。
- 透明判断。
- 生成 `SpriteDocument`。

入口包括：

- 现成精灵图导入。
- 普通图片像素化。

可以先简化：

- 只支持 `16x16`、`32x32`、`64x64` 三档固定尺寸，不做任意自定义尺寸。
- 不做复杂调色板优化。
- 不做多种像素化风格。

### 阶段 5：sprite sheet 导出

目标：在 JSON / PNG 稳定后补齐动画导出。

必须完成：

- 按帧顺序导出 sprite sheet PNG。
- 保留透明背景。
- 保证每帧尺寸一致。
- 导出信息能表达帧宽高、帧顺序、动作名、fps、是否循环。

可以延后：

- 多行多列配置。
- 每帧间距。
- 边距设置。
- 多动作合并导出。

### 阶段 5.5：工程文件导入 / 导出

目标：提供本地备份和跨设备手动迁移能力，先不做云同步。

工程文件建议使用：

```text
.sprite.json
```

本质是完整 `SpriteDocument`。

必须完成：

- 导出完整 `SpriteDocument` 为 `.sprite.json`。
- 导入 `.sprite.json` 并校验结构。
- 导入后创建新项目，不直接覆盖已有项目。
- 校验 `schemaVersion`、尺寸、动作、帧、像素格式和颜色值。

可以延后：

- 批量导入。
- 批量导出。
- 工程文件压缩包。
- 云端备份。

### 阶段 6：AI 生成入口

目标：AI 只负责创建初始图片或初始文档，不进入编辑器核心。

建议先做假入口：

- 提示词输入 UI。
- 生成中状态。
- 固定示例图模拟生成结果。

核心编辑器稳定后，再接真实 AI。

真实接入时：

- API Key 放后端。
- 生成结果先进入像素化流程。
- 用户确认后再写入 `SpriteDocument`。
- AI 失败不能影响已保存项目。

## 6. 测试和验收方案

### 阶段 1 验收：数据结构和基础画布

- 能生成默认 `SpriteDocument`。
- Canvas 能按 `SpriteDocument` 正确渲染。
- 单个像素块可以被选中、改色、清空透明。
- 编辑后 `SpriteDocument` 数据同步变化。
- 保存后刷新页面，再重新打开，内容不丢失。

### 阶段 2 验收：项目存档

- 可以创建项目、命名项目、保存项目。
- 项目存档页能列出已有项目。
- 点击项目后能回到编辑器，并恢复画布内容。
- 修改后再次保存，不生成重复脏数据。
- 重命名后，项目卡片和完整文档名称一致。
- 删除项目必须二次确认，删除后不能误删其他项目。
- 卡片缩略图来自真实项目内容，不是固定占位图。
- 刷新页面后，项目列表仍然存在。
- 空项目、损坏项目、缺字段项目有明确提示。

### 阶段 3 验收：动作和帧

- 可以创建、切换、重命名和删除动作。
- 每个动作最多支持 12 帧。
- 新增、复制、删除帧行为正确。
- 至少保留 1 帧。
- 播放预览帧顺序正确。
- 当前帧编辑不会误改其他帧或其他动作。

### 阶段 4 验收：导入和像素化

- 普通图片可以像素化成可编辑精灵图。
- 现成精灵图可以转成可编辑像素块。
- 非方形图片、超大图片、格式不支持时有清楚反馈。
- 透明背景能正确保留或处理。
- 像素化失败时页面不崩、不丢已保存项目。

### 阶段 5 验收：导出

- JSON 导出后重新导入，画面和数据一致。
- PNG 导出清晰，不模糊，不出现像素缝。
- sprite sheet 导出帧数、排列、透明背景、尺寸正确。
- 导出前后的项目数据不被意外修改。

## 7. 需要浏览器证据的点

- 编辑器首屏截图：证明深色紫色像素风、画布和工具栏布局成立。
- 项目存档页截图：证明项目卡片、空状态、打开入口清楚。
- Canvas 渲染截图：证明进入的是可编辑像素画布。
- 单像素编辑前后截图：证明可以逐像素修改。
- 保存项目后重新打开截图：证明存档真的可恢复。
- JSON 导出文件片段：证明存在真实 `SpriteDocument` 数据。
- PNG 导出结果截图：证明像素边缘清晰。
- sprite sheet 导出结果截图：证明帧排列和透明背景正确。
- 浏览器控制台记录：确认核心流程没有明显报错。

## 8. 移动端范围

V1 移动端不承诺完整专业编辑能力，只保证基础可用：

- 能打开项目存档页。
- 能进入编辑器查看精灵图。
- 能放大、缩小、拖动画布。
- 能选中像素并完成简单改色或清空。
- 能保存项目。
- 能看到导出入口，至少不被遮挡。

不强求移动端完成高效率逐像素创作，不强求复杂 sprite sheet 配置在移动端全部可操作。

## 9. 主要风险和规避

### 9.1 SpriteDocument 设计不稳

风险：后面保存、导出、AI、图片像素化都会返工。

规避：第一阶段先把字段压到最小，但保留帧、图层、动画的扩展位置。

### 9.2 Canvas 点击坐标错位

风险：用户点 A 格，实际改到 B 格。

规避：先固定缩放比例和画布尺寸，确认坐标计算稳定后再做缩放拖拽。

### 9.3 IndexedDB 保存读取不可靠

风险：显示保存成功，但重新打开数据丢失。

规避：首轮只做最简单的保存、读取、覆盖，并把刷新后恢复作为硬验收。

### 9.4 导出结果和编辑画面不一致

风险：用户编辑了画布，但导出的 JSON、PNG、sprite sheet 不一致。

规避：所有导出都从 `SpriteDocument` 生成，不从页面截图凑结果。

### 9.5 AI 质量阻塞核心编辑器

风险：AI 生成不稳定，导致编辑器主线无法推进。

规避：AI 放到最后阶段，先用假入口和示例图跑通编辑闭环。

### 9.6 项目摘要和完整文档不同步

风险：项目卡片显示的是旧缩略图、旧名称或旧时间，但打开后是另一份内容。

规避：保存项目时统一由存储层同时更新 `ProjectSummary` 和 `SpriteDocument`，组件不要分别手写更新逻辑。

### 9.7 云同步过早引入

风险：本地存档还没稳定，就引入账号、冲突、网络失败，会把问题复杂化。

规避：V1 只做本地 IndexedDB 和 `.sprite.json` 工程文件导入 / 导出；云同步只预留字段和边界。

## 10. 用户确认状态

### 10.1 已确认

- 第一轮按“最小闭环”推进，不一次做完整 V1。
- V1 尺寸固定为 `16x16`、`32x32`、`64x64` 三档，并默认推荐 `64x64`。
- V1 支持多个动作类型，不限制为 `idle`。
- V1 每个动作最多 12 帧。
- V1 只开放一个默认图层，但数据结构预留多图层。
- V1 必须导出 `JSON`、`PNG`、`sprite sheet PNG`。
- 第一轮 AI 只做占位入口，不接真实 AI。
- 项目存档先做本地 IndexedDB，不做云同步。
- 项目存档第一轮必须支持新建、打开、保存、重命名、删除。
- 复制项目、复杂搜索、项目分组、自动保存和云同步延后。

### 10.2 仍待确认

- 暂无。
