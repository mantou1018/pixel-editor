# 像素图生成器开发记录

## 2026-06-09：文字生成像素图工作流第一阶段

### 变更

- 新增 `src/domain/sprite/generate.ts`：
  - 定义 `PixelGenerationStyle`、生成请求和候选类型。
  - 用提示词、尺寸和风格生成 4 个稳定候选。
  - 候选结果直接生成 `SpriteDocument`，可进入编辑器继续修改。
- 更新 `src/app/projects/page.tsx`：
  - 增加“文字生成像素图”区域。
  - 支持提示词输入、风格选择、生成候选、选择候选进入编辑器。
- 更新 `src/app/globals.css`：
  - 增加生成面板、风格选择、候选卡片和移动端布局样式。
- 更新 `src/app/editor/[projectId]/page.tsx`：
  - 右侧面板显示生成来源。
  - 把原来的“AI 生成占位”改为“基于当前图再生成”的下一阶段入口说明。
- 新增 `src/app/icon.svg`：
  - 提供应用图标，避免浏览器 favicon 请求 404。
- `next-env.d.ts`：
  - Next 构建自动把类型引用从 `.next/dev/types/routes.d.ts` 更新为 `.next/types/routes.d.ts`。

### 说明

当前生成逻辑是本地占位，不调用真实 AI。它的价值是先把“输入描述 -> 生成候选 -> 选择结果 -> 编辑导出”的产品闭环跑通。

## 2026-06-10：PNG 导出尺寸和背景设置

### 变更

- 更新 `src/domain/sprite/render.ts`：
  - `renderDocumentToCanvas` 支持导出前填充背景色。
  - `exportDocumentPng` 支持传入 `scale` 和 `backgroundColor`。
- 更新 `src/app/editor/[projectId]/page.tsx`：
  - 增加导出尺寸设置：原始尺寸 / 预览大图。
  - 增加导出背景设置：透明背景 / 当前颜色背景。
  - 导出文件名带尺寸后缀，例如 `64x64` 或 `512px-preview`。
  - 导出后状态栏显示本次导出的尺寸和背景。
- 更新 `src/app/globals.css`：
  - 增加导出设置按钮组样式和选中态。

### 说明

这次改动解决的是“像素图交付出口”问题：真正用于游戏或素材库的文件应能保持原始尺寸和透明背景；用于展示的文件可以选择放大图和纯色背景。

## 2026-06-10：PNG 导入调色板归并

### 变更

- 更新 `src/domain/sprite/importPng.ts`：
  - 增加 `ImportPaletteSize` 和 `PngImportOptions`。
  - PNG 导入支持 8 / 16 / 32 / 64 / 128 / 256 / 512 色调色板数量。
  - 在平均取色后统计主色，并把每个像素映射到最近的主色。
  - 来源记录会写入实际调色板数量，例如 `PNG 导入：xxx.png，16 色`。
- 更新 `src/app/projects/page.tsx`：
  - 在 PNG 导入区域增加调色板数量选择。
  - 导入状态文案显示当前尺寸和颜色数量。
- 更新 `src/app/globals.css`：
  - 增加导入调色板按钮组样式和选中态。

### 说明

这次改动把“像素化控制”从纯内部算法变成用户可操作参数。它先解决颜色碎的问题，不引入复杂滤镜和新依赖。

## 2026-06-10：生成历史、收藏候选与用户路径测试

### 变更

- 更新 `src/app/projects/page.tsx`：
  - 新增生成历史，保留最近 6 轮页面内生成结果。
  - 历史项支持“再次使用”，会恢复提示词、尺寸、风格和候选。
  - 候选卡片新增收藏按钮。
  - 收藏后显示“收藏候选”区域，可从收藏直接进入编辑器。
  - 收藏夹按钮改为“编辑此收藏”，避免与页面其他“编辑”文案混淆。
- 更新 `src/app/globals.css`：
  - 增加候选操作区、收藏夹、历史列表的布局和移动端样式。

### 测试视角发现

- 已修：收藏夹里的“编辑”按钮文案太泛，自动化和辅助技术容易匹配到很多“编辑”相关文本；已改为“编辑此收藏”。
- 未修：生成历史和收藏仍是页面内状态，刷新或离开项目页会消失。若用户确认这条交互有价值，应进入 IndexedDB 持久化。

## 2026-06-10：生成工作台持久化

### 变更

- 更新 `src/lib/storage/projects.ts`：
  - IndexedDB 版本从 1 升到 2。
  - 新增 `generation_workspace` 存储区。
  - 新增 `loadGenerationWorkspace` 和 `saveGenerationWorkspace`，保存生成历史和收藏候选 ID。
- 更新 `src/app/projects/page.tsx`：
  - 页面加载时恢复最近生成历史、收藏候选、提示词、尺寸、风格和当前候选。
  - 生成历史或收藏变化后自动写入本机浏览器。
  - 生成按钮旁增加说明：历史和收藏保存在本机浏览器。

### 说明

这次修复的是用户工作流连续性，不是云端同步。它保证刷新项目页后还能继续选择、再次使用或编辑收藏，但换浏览器或清空浏览器数据后不会保留。

## 2026-06-10：PNG 导入增加 512 色上限

### 变更

- 更新 `src/domain/sprite/importPng.ts`：
  - `ImportPaletteSize` 增加 128 / 256 / 512。
  - 调色板硬上限从 64 提高到 512。
- 更新 `src/app/projects/page.tsx`：
  - PNG 导入调色板按钮增加 128 色、256 色、512 色。

### 说明

这次改动回应“颜色太少”的问题。较高色数更适合保留普通图片细节；低色数仍保留，用于强像素风压色。

## 2026-06-10：同级选项折叠展示

### 变更

- 更新 `src/app/projects/page.tsx`：
  - 新增折叠选项状态。
  - 尺寸、PNG 导入调色板数量、生成风格默认只显示当前选项。
  - 点击箭头展开其它选项，选择后自动收起。
- 更新 `src/app/globals.css`：
  - 新增紧凑选项组、箭头按钮、展开列表和风格选项样式。
  - 清理旧的平铺选项样式。

### 说明

这次改动是为减少页面噪音，不改变生成、导入或编辑数据逻辑。

## 2026-06-11：同级选项改为 PC 悬浮下拉

### 变更

- 更新 `src/app/projects/page.tsx`：
  - 将下拉箭头文案从 `⌄` 调整为 `▾`。
- 更新 `src/app/globals.css`：
  - 选项组增加相对定位。
  - 更多选项列表改为绝对定位浮层，带背景、边框、阴影和更高层级。
  - 展开箭头增加旋转反馈。

### 说明

这次修正交互形态：更多选项是 PC 浮层下拉，不再把页面内容往下推。

## 2026-06-11：按 REVIEW P0 优化主流程交互

### 变更

- 更新 `src/app/projects/page.tsx`：
  - 把“文字生成像素图”上移为项目页首屏主工作台。
  - 把“创建空白项目”和“PNG 导入”降级为“从已有素材开始”辅助入口。
  - 下拉控件支持点击外部关闭、Esc 关闭。
  - 点击生成前自动收起展开中的选项浮层。
  - 页面标题 eyebrow 从 `Sprite Archive` 改为 `Pixel Sprite Studio`，移除没有真实意义的 `第01/01页`。
- 更新 `src/components/editor/SpriteCanvas.tsx`：
  - 默认缩放从固定值改为按当前可见视口 fit-to-view。
  - 增加 `- / 适应 / +` 缩放控件。
  - 画布容器变化时自动重新计算适应缩放。
- 更新 `src/app/editor/[projectId]/page.tsx`：
  - 顶部主操作保留“保存”和“导出 PNG”，把 JSON 导出放入“更多”。
  - 右侧面板优先展示“导出设置”和“图层”，来源、颜色、动作/帧信息下沉到“信息”区。
  - 把用户可见的 `文字生成占位` 替换为 `文字生成`。
- 更新 `src/app/globals.css`：
  - 调整项目页信息层级、生成工作台、辅助入口、上传区、候选卡高度、编辑器更多菜单、画布缩放控件和移动端布局。

### 验证

- `npm run build` 通过。
- 桌面端 `1280x720`：项目页首屏以生图为主；风格下拉不遮挡生成按钮；Esc 可关闭；生成、进入编辑器、导出 PNG 跑通。
- 编辑器桌面端：64x64 图首屏完整可见，导出设置为右侧第一面板，JSON 不再作为顶级按钮。
- 移动端 `390x844`：项目页和编辑器均无横向溢出；项目页首屏可见生成入口；编辑器首屏可完整看到图并可导出。

### 遗留

- 候选卡还缺少“推荐 / 轮廓更清晰 / 颜色更少”等决策辅助标签。
- PNG 导入还没有做导入前预览。
- 移动端编辑器目前只达到可查看、可缩放、可导出的门槛；完整触控绘制体验需要单独设计。

## 2026-06-11：项目页改为左右工作区

### 变更

- 更新 `src/app/projects/page.tsx`：
  - 给“文字生成像素图”和“已有素材编辑”增加同一个工作区外层。
  - “从已有素材开始”标题改为“已有素材编辑”，语义更直接。
- 更新 `src/app/globals.css`：
  - 新增 `.workspace-layout`，桌面端使用左右两栏。
  - 左侧生图区占主要宽度，右侧已有素材编辑区保持紧凑。
  - 候选结果从拉伸网格改为固定 `280px` 卡片，容器最大宽度 `1168px`，桌面端一行最多容纳 4 个。
  - `980px` 以下回到上下堆叠，保持移动端可读性。

### 验证

- `npm run build` 通过。
- 桌面端 `1280x720` 已确认左右并列且无横向溢出。
- 桌面端 `1280x720` 已确认当前 3 个候选均保持 `280px` 宽，左对齐，不会顶满拉伸。
- 移动端 `390x844` 已确认上下堆叠且无横向溢出。

## 2026-06-11：修复画布手动放大会回弹

### 变更

- 更新 `src/components/editor/SpriteCanvas.tsx`：
  - 新增画布缩放模式：`fit` 和 `manual`。
  - 初次进入编辑器和点击“适应”时使用 `fit` 模式。
  - 用户点击 `+`、`-` 或使用滚轮缩放后切到 `manual` 模式。
  - `ResizeObserver` 只在 `fit` 模式下重新计算适应缩放，不再覆盖手动缩放。
  - 画布状态文案增加“适应窗口 / 手动缩放”，让用户知道当前缩放规则。

### 原因

之前 `ResizeObserver` 会在画布或容器尺寸变化时自动执行 `fitCanvasToShell()`。用户手动放大后，画布尺寸变化触发 observer，缩放又被重设为适应窗口大小，所以看起来像“放大后回弹”。

### 验证

- `npm run build` 通过。
- 连续点击 `+` 6 次后，缩放从 `4x` 到 `10x`。
- 等待 1.5 秒仍保持 `10x · 手动缩放`。
- 点击“适应”后才回到 `4x · 适应窗口`。

## 2026-07-05：像素无限画布工作区地基

### 变更

- 更新 `src/domain/sprite/types.ts`：
  - `SpriteSize` 从 `16 / 32 / 64` 扩展为 `16 / 32 / 64 / 128 / 256`。
- 更新 `src/domain/sprite/importPng.ts`：
  - 图片导入从仅 PNG 放宽为 PNG / JPG / WebP。
  - 用户可见文案从 PNG 导入调整为图片导入。
  - 保留原始图片 dataUrl、文件名、像素分辨率和调色板数量，后续重新像素化可以从原图计算。
- 新增 `src/domain/workspace/types.ts` 和 `src/domain/workspace/document.ts`：
  - 定义 `CanvasWorkspace`、`WorkspaceItem`、`WorkspaceSummary`。
  - 每个画布对象保存位置、显示大小、像素设置、来源和内部 `SpriteDocument`。
- 更新 `src/lib/storage/projects.ts`：
  - IndexedDB 版本从 2 升到 3。
  - 新增 `workspace_summaries` 和 `workspace_documents` 两个 store。
  - 新增工作区列表、读取、保存、删除函数。
- 更新 `src/app/projects/page.tsx`：
  - 尺寸选项增加 `128x128` 和 `256x256`。
  - “PNG 导入”入口改为“图片导入”，文件选择支持 PNG / JPG / WebP。
- 更新 `src/app/editor/[projectId]/page.tsx`：
  - 重新像素化尺寸增加 `128x128` 和 `256x256`。
  - 相关文案从“原 PNG”调整为“原图片”。

### 说明

这次不是完整无限画布 UI，而是先补数据和导入地基。下一步可以基于 `CanvasWorkspace` 做可保存工作区页面，再把多图片对象渲染到 Figma 式画布里。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/projects` 返回 `HTTP/1.1 200 OK`。

### 遗留

- 还没有实现无限画布页面。
- 还没有把工作区保存能力接到 UI。
- 还没有做多对象导入、移动、选择和单对象导出。

## 2026-07-06：新增可打开的像素无限画布页面

### 变更

- 新增 `src/app/workspace/page.tsx`：
  - 提供 `/workspace` 路由。
  - 支持导入 PNG / JPG / WebP 图片，并自动生成画布对象。
  - 支持在画布中选择和拖动对象。
  - 支持当前对象切换 `16x16`、`32x32`、`64x64`、`128x128`、`256x256`。
  - 支持调整调色板数量并重新像素化。
  - 支持保存工作区和导出当前对象 PNG。
- 更新 `src/app/projects/page.tsx`：
  - 顶部增加“打开像素无限画布”入口。
- 更新 `src/app/globals.css`：
  - 新增无限画布页面、对象列表、画布对象和右侧检查器样式。

### 原因

用户打开 `/projects` 后看到的仍是旧项目存档页。上一轮只完成了数据地基，没有把可见的无限画布工作台接到路由上。本次补上可直接访问的 `/workspace` 页面。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。

### 遗留

- 画布当前是基础拖动和选择，还没有做滚轮缩放、框选、对齐线和多选。
- 工作区保存已接入，但还没有做工作区列表和重新打开入口。
- Playwright 浏览器二进制未安装，未做自动截图验证。

## 2026-07-06：画布查看缩放

### 参考

- Figma / FigJam 的画布查看核心是：缩放百分比、加减缩放、适应内容，以及通过修饰键配合滚轮缩放。
- 本轮先实现查看层能力，不扩展完整 Figma 快捷键体系。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 新增画布缩放状态，范围为 25% 到 400%。
  - 新增 `- / 100% / + / 适应` 缩放控件。
  - 支持 `Cmd/Ctrl + 滚轮` 按鼠标位置缩放。
  - 对象拖动按当前缩放比例换算，避免放大后拖动距离失真。
  - “适应”会把全部对象纳入当前视野；无对象时回到 100%。
- 更新 `src/app/globals.css`：
  - 画布增加 viewport 和 scaled stage。
  - 画布内容使用 `transform: scale(...)` 缩放。
  - 缩放控件悬浮在画布右上角。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 打开 `http://localhost:3000/workspace`，首屏显示“像素无限画布”和 `- / 100% / + / 适应` 缩放控件。

### 遗留

- 还没有做空格键临时抓手、`Shift + 1` / `Shift + 2` 快捷键和框选缩放。
- 普通滚轮目前保留浏览器滚动查看，不拦截成画布平移。

## 2026-07-06：对照 Figma 收紧画布布局和导航

### 参考

- Figma 工作台核心布局是紧凑顶部工具栏、左侧对象/图层、中央画布主区域、右侧属性面板。
- 画布导航核心是选择工具、抓手工具、空格临时抓手、适应全部和适应选中。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 新增选择 / 抓手工具状态。
  - 支持按 `V` 切回选择工具。
  - 支持按 `H` 切到抓手工具。
  - 支持按住空格临时使用抓手。
  - 支持拖动画布空白区域平移视口。
  - 支持 `Shift + 1` 适应全部对象。
  - 支持 `Shift + 2` 适应选中对象。
  - 抓手模式下点击对象不会移动对象，而是继续平移画布。
- 更新 `src/app/globals.css`：
  - 顶部从大标题区改成紧凑工具栏。
  - 左侧对象栏和右侧属性栏收窄。
  - 画布区域占据更大视觉面积。
  - 抓手模式显示 `grab / grabbing` 光标。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 打开 `http://localhost:3000/workspace`：
  - 页面一屏显示，无整体上下滚动。
  - 顶部显示选择 / 抓手工具。
  - 按 `H` 后抓手工具选中。
  - 按住空格时画布 viewport 进入 `hand-active` 状态。
  - 按 `V` 后选择工具选中。
  - 画布右上角显示 `- / 100% / + / 适应`。

### 遗留

- 还没有做对象多选、框选和对齐线。
- 还没有把普通滚轮映射为横纵平移。

## 2026-07-06：Figma 核心画布交互第一批

### 范围判断

用户要求“100% 还原 Figma 的画布功能和交互”。团队收敛后判断：不能承诺完整 Figma 编辑器 100% 复刻，但可以优先还原 Figma 式画布交互层，包括选择、平移、缩放、多选、框选、复制、删除和方向键移动。

### 变更

- 更新 `src/domain/workspace/document.ts`：
  - 新增多选、反选、移动选中对象、删除选中对象、复制选中对象等工作区工具函数。
- 更新 `src/app/workspace/page.tsx`：
  - 支持 `Shift / Cmd / Ctrl + 点击` 多选。
  - 支持多选对象整体拖动。
  - 支持从画布空白处拖出框选区域。
  - 支持 `Delete / Backspace` 删除选中对象。
  - 支持 `Cmd/Ctrl + C` 复制选择源，`Cmd/Ctrl + V` 粘贴副本。
  - 支持 `Cmd/Ctrl + D` 直接复制选中对象。
  - 支持方向键移动选中对象，`Shift + 方向键` 大步移动。
  - 导入多张图片时按网格错开摆放，避免对象互相遮住导致无法点选。
  - 多选时右侧面板显示批量状态和复制 / 删除操作。
- 更新 `src/app/globals.css`：
  - 新增选中数量标签和框选矩形样式。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 导入 2 张测试图片后，画布出现 2 个对象。
  - `Shift + 点击` 后 2 个对象均进入选中态。
  - `Cmd + C`、`Cmd + V` 后对象数从 2 变为 4。
  - `Delete` 后对象数从 4 回到 2。

### 遗留

- 还没有撤销 / 重做。
- 还没有对齐线、吸附、编组、右键菜单和图层锁定隐藏。
- 还没有完整触控板手势和 Figma 全量快捷键。

## 2026-07-06：放大画布缩放范围

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 画布缩放范围从 `25% - 400%` 改为 `2% - 6400%`。
  - 缩放按钮改为倍率乘法缩放，更适合大范围缩放。
  - 低倍率百分比显示支持小数，例如 `2.0%`。
  - 增加 `Cmd/Ctrl + +` 放大。
  - 增加 `Cmd/Ctrl + -` 缩小。
  - 增加 `Cmd/Ctrl + 0` 适应全部。
  - 增加 `Cmd/Ctrl + 1` 回到 100%。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 验证：
  - 连续缩小可到 `2.0%`。
  - 连续放大可到 `6400%`。
  - `Cmd + 1` 可回到 `100%`。

## 2026-07-06：画布区域接管妙控板缩放

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 将 `Ctrl / Cmd + wheel` 缩放监听提升到 window capture 阶段。
  - 只有事件目标在 `.canvas-viewport` 内时才拦截。
  - 在画布区域内阻止浏览器默认网页缩放，改为缩放画布。
  - 阻止 Safari / Chrome 常见的 `gesturestart / gesturechange / gestureend` 默认行为，避免页面整体缩放。

### 验证

- `npm run build` 通过。
- build 后单独执行 `npx tsc --noEmit` 通过。
- 使用系统 Chrome 验证：
  - 鼠标位于画布区域内。
  - 按住 Control 并滚轮后，画布缩放从 `100%` 变为 `112%`。
  - `window.visualViewport.scale` 仍为 `1`，网页没有整体缩放。

### 说明

- 第一次并行执行 `npx tsc --noEmit` 和 `npm run build` 时，`tsc` 命中了 Next 生成类型文件竞态；build 通过后单独重跑 `tsc` 已通过。

## 2026-07-06：修复低倍缩放白块并收敛缩放逻辑

### 调研结论

- Figma 的画布缩放重点是“鼠标/触控板在画布内时缩放画布视图”，而不是缩放整个网页。
- tldraw 这类无限画布会用 `zoomSteps` 一类的固定缩放档位，首尾档位就是最小和最大缩放。
- Excalidraw 相关讨论也强调滚轮缩放需要有速度感和上限，不能无边界放大缩小。
- 因此本项目采用：画布位置可持续平移，缩放有上下限；按钮和快捷键走固定档位，触控板滚轮走连续缩放但被同一套上下限夹住。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 将 `minCanvasScale` / `maxCanvasScale` 改为从 `zoomSteps` 首尾值自动取得，避免缩放极限和按钮档位不一致。
  - 增加 `getNextZoomStep`，让缩放按钮使用成熟画布常见的固定档位。
  - 画布内 `Cmd/Ctrl + wheel` 改为按滚轮距离连续缩放，并继续围绕鼠标位置缩放。
- 更新 `src/app/globals.css`：
  - 移除 `.canvas-grid-surface` 自身背景，避免低倍缩小时有限舞台露出白色矩形。
  - 让 `.canvas-viewport` 负责铺满无限网格背景。
  - 给网格背景增加 `8px` 最小间距，避免 2% / 4% 低倍缩放时网格糊成一片。

### 验证

- `npm run build` 通过。
- `npx tsc --noEmit` 通过。
- 使用系统 Chrome 自动验证：
  - 缩放到 `2.0%` 时，原本截图中左上角白块位置仍属于 `.canvas-viewport`，不再是有限白底层。
  - `.canvas-grid-surface` 背景为 `none`。
  - 低倍缩放时网格背景尺寸保持 `8px` 最小间距。
  - 鼠标在画布内按住 Control 滚轮后，画布从 `100%` 变为 `272%`，`window.visualViewport.scale` 仍为 `1`，网页没有整体缩放。

## 2026-07-07：当前像素对象内编辑工具第一版

### 需求收敛

- 编辑范围：只编辑当前选中的像素对象，类似 Figma 中先选中 frame / 对象后进入对象内部编辑。
- 智能选择：第一版采用稳定的魔棒式选择区域，不接 AI 主体识别。
- 编辑安全：支持撤销 / 重做，避免笔刷和橡皮误操作后无法恢复。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 顶部工具栏新增：笔刷、橡皮、吸管、填充、魔棒、撤销、重做。
  - 笔刷支持 `1px / 2px / 4px / 8px`，在当前选中对象内按像素格绘制。
  - 橡皮将当前对象内像素擦为透明。
  - 吸管可从当前对象取色，取色后自动回到笔刷。
  - 填充工具填充相邻同色区域。
  - 魔棒复用 `createSmartSelection`，生成当前对象内部的像素选区。
  - 魔棒选区存在时，笔刷 / 橡皮 / 填充只影响选区内像素。
  - `Cmd/Ctrl + Z` 撤销，`Cmd/Ctrl + Shift + Z` 重做。
- 更新 `src/app/globals.css`：
  - 工具栏支持横向收纳，避免工具增多后撑坏顶部布局。
  - 新增对象内编辑控制、颜色选择、笔刷大小和魔棒选区覆盖层样式。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 导入图片后画布出现 1 个像素对象并自动选中。
  - 顶部出现笔刷工具，右侧出现“对象内编辑”面板。
  - 笔刷拖动画后，右侧预览图发生变化，状态显示“已绘制像素”。
  - 吸管点击像素后取到颜色，并自动切回笔刷。
  - 魔棒点击对象后生成 1702 个像素选区，并显示“清除魔棒选区”入口。
  - `Cmd + Z` 后状态显示“已撤销上一步编辑”。

### 遗留

- 还没有单独的“进入 / 退出对象编辑模式”面包屑。
- 笔刷目前是方形像素笔刷，还没有圆形笔刷、硬度、混合模式。
- 魔棒暂无用户可调容差；当前使用现有智能选择阈值。
- 还没有右键菜单、吸附、编组、图层锁定隐藏等完整 Figma / PS 工作台能力。

## 2026-07-07：画布缩放丝滑化

### 需求收敛

- 用户反馈“缩放画布不够丝滑”，确认优化范围为 C：触控板 / 鼠标滚轮缩放和按钮缩放都要优化。
- 优先目标是让画布缩放更连续、更跟手，同时保持浏览器页面本身不被缩放。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 画布缩放精度从 2 位小数提升到 4 位小数，避免小幅触控板滚动被吞掉。
  - 增加 `canvasScaleRef`，避免密集滚轮事件读取旧缩放值。
  - 增加 `wheelZoomRef`，用 `requestAnimationFrame` 合并同一帧内的 wheel 事件，降低抖动。
  - 缩放速度从 `0.002` 调低到 `0.0012`，小幅滚动更细腻。
- 更新 `src/app/globals.css`：
  - 画布舞台 `transform / width / height` 增加 `90ms ease-out` 短过渡。
  - 网格背景尺寸增加 `90ms ease-out` 短过渡，减少网格跳动感。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 打开 `http://localhost:3000/workspace` 成功。
  - 画布内按住 Control 连续小幅滚轮后，缩放从 `100%` 到 `119%`。
  - 实际 CSS 缩放值为 `1.1886`，说明已不是粗糙两位小数跳变。
  - `window.visualViewport.scale` 保持 `1`，浏览器页面没有整体缩放。
  - 点击 `+` 后缩放到 `150%`，画布层存在 `transform 0.09s ease-out` 过渡。

## 2026-07-07：修复魔棒选区切换工具后消失

### 问题

- 用户反馈：魔棒选择区域后，点击橡皮或画笔，魔棒区域会消失。
- 根因：工具切换函数里只允许 `magic` 工具保留选区，切到 `brush / eraser / fill / eyedropper` 时会清空 `magicSelection`。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 增加 `pixelEditTools` 工具组：`brush / eraser / eyedropper / fill / magic`。
  - 在这些对象内编辑工具之间切换时保留魔棒选区。
  - 切到 `select / hand` 时仍清除魔棒选区，表示离开对象内部编辑。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 魔棒点击对象后生成 1703 个选区格。
  - 切换到橡皮后选区仍为 1703 个。
  - 切换到笔刷后选区仍为 1703 个。

## 2026-07-07：优化笔刷和橡皮跟手感与工具光标

### 问题

- 用户反馈：笔刷和橡皮不跟手，并希望使用时有对应图标显示。
- 根因：拖动画笔时只处理浏览器派发的离散 pointer 点，快速拖动时中间像素没有补齐，容易出现断线和漏格。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 给 `pixelPaintRef` 增加 `lastPoint`，记录上一帧绘制到的像素位置。
  - 新增 `getLinePoints`，在上一个像素点和当前像素点之间做线段插值。
  - 笔刷 / 橡皮拖动时会补齐中间经过的像素，减少快速拖动断线。
  - 新增 `getPaintCursor`，笔刷显示笔刷 SVG 光标，橡皮显示橡皮 SVG 光标，其它像素工具保留十字光标。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 切到笔刷后，当前选中像素对象的 CSS cursor 为内嵌笔刷 SVG。
  - 快速拖动画笔后，右侧预览发生变化，状态显示“已绘制像素”。
  - 切到橡皮后，当前选中像素对象的 CSS cursor 为内嵌橡皮 SVG。

## 2026-07-07：修复画笔尖和落点偏移

### 问题

- 用户通过录屏反馈：画笔尖离实际画出的像素有一定距离。
- 根因：像素对象有 `padding: 14px`，真实图片内容区比对象外框小；之前坐标换算使用对象整体 `width / height`，导致笔尖视觉位置和实际像素落点不一致。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 新增 `workspaceItemPadding`，与像素对象内容内边距对齐。
  - `getItemPixelPoint` 改为按真实图片内容区计算像素坐标，而不是按对象外框计算。
  - 调整笔刷 SVG cursor 热点到笔尖位置。
  - 调整橡皮 SVG cursor 热点到橡皮接触区域。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 当前对象外框为 `256x256`，图片内容区为 `222x222`，存在约 `17px` 内缩。
  - 点击图片内容区中心后，右侧预览发生变化，状态显示“已绘制像素”。
  - 笔刷 cursor 热点为 `3 25`。
  - 橡皮 cursor 热点为 `13 17`。

## 2026-07-07：修复魔棒选区边线交叉

### 问题

- 用户截图反馈：魔棒选区显示成长宽不一的长方形，边线还互相交叉。
- 根因：此前每个被选中的像素格都画完整 `outline`，相邻像素之间也会画内部边线；多个格子叠在一起后看起来像蓝色网格交叉。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 新增 `getSelectionCellBorders`。
  - 每个选区像素只在“外侧没有相邻选区像素”的方向画边线。
  - 选区内部相邻边不再画线，只保留淡蓝填充。
- 更新 `src/app/globals.css`：
  - 移除选区像素的 `outline`。
  - 改用 `border-width` 四方向 CSS 变量绘制外轮廓。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 魔棒选区生成 1703 个像素格。
  - 选区格 `outline-style` 为 `none`。
  - 选区格边框按 `top / right / bottom / left` 四方向变量出现。
  - 样式中存在 `0px 0px 0px 0px` 内部像素，说明内部相邻线已被抑制。

## 2026-07-08：修复工作区对象背景套娃

### 问题

- 用户截图反馈：像素对象背景看起来“一层套一层”。
- 根因：工作区对象容器已经有透明棋盘格背景，内部缩略图 `renderDocumentThumbnail` 默认又绘制了一层暗色棋盘格，导致透明区域出现双层背景。

### 变更

- 更新 `src/domain/sprite/render.ts`：
  - `renderDocumentThumbnail` 增加可选参数 `showCheckerboard`。
  - 默认仍保持 `true`，避免影响项目页候选图等旧界面。
- 更新 `src/app/workspace/page.tsx`：
  - 工作区对象、左侧列表和右侧预览使用 `showCheckerboard: false` 的透明缩略图。
  - 透明背景只由外层 UI 容器负责显示。

### 验证

- `npm run build` 通过。
- build 后单独执行 `npx tsc --noEmit` 通过。
- 使用系统 Chrome 自动验证：
  - 工作区对象图片 data URL 仍为 PNG。
  - 图片自身存在透明采样点，alpha 为 `0`。
  - 外层 `.workspace-canvas-item` 仍保留棋盘格背景。
  - 因此工作区中只剩外层一层透明背景，不再内外叠加。

## 2026-07-08：新增清空选区内容和浅灰像素网格

### 需求收敛

- 一键清空选区内容：针对当前魔棒选区，将选区内像素擦为透明，不删除对象，不取消选区。
- 网格展示 / 隐藏：针对当前选中的像素对象显示像素格辅助线，网格为浅灰色，只是编辑辅助层。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 新增 `showPixelGrid` 状态。
  - 新增 `clearSelectedPixels`，把当前魔棒选区内像素批量设置为透明，并进入撤销历史。
  - 右侧“对象内编辑”区域新增“显示网格 / 隐藏网格”按钮。
  - 魔棒选区存在时新增“清空选区内容”和“取消选区”两个明确操作。
  - 选中对象上新增 `.workspace-pixel-grid` 覆盖层。
- 更新 `src/app/globals.css`：
  - 新增浅灰色像素网格样式。
  - 新增选区操作按钮布局。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 点击“显示网格”后，选中对象出现 `.workspace-pixel-grid`。
  - 网格背景为 `rgba(156, 163, 175, 0.42)` 的浅灰色线。
  - 点击魔棒后生成 2707 个选区像素。
  - 点击“清空选区内容”后，右侧预览发生变化。
  - 状态显示“已清空选区内容：2707 个像素”。
  - 点击“隐藏网格”后，`.workspace-pixel-grid` 消失。

## 2026-07-08：提高触控板缩放灵敏度

### 问题

- 用户反馈：触控板缩放不够灵敏。
- 判断：此前为了让缩放更细腻，把连续滚轮缩放速度从 `0.002` 调到 `0.0012`；现在实际手感偏慢，需要在细腻和响应之间取中间值。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 将 `wheelZoomSensitivity` 从 `0.0012` 调整为 `0.0018`。
  - 保持 `requestAnimationFrame` 合并滚轮事件。
  - 保持以鼠标 / 触控板指针所在位置为中心缩放。
  - 不改变 `- / + / 适应` 按钮缩放档位。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。
- 使用 Codex 自带 Playwright CLI wrapper 验证：
  - 打开 `http://localhost:3000/workspace` 后，初始缩放为 `100%`。
  - 鼠标移动到 `.canvas-viewport` 中心，按住 Control 后连续 8 次小幅 wheel。
  - 缩放显示变为 `77%`，CSS 变量 `--canvas-scale` 为 `0.7717`。
  - `window.visualViewport.scale` 保持 `1`，浏览器页面没有整体缩放。
  - `window.scrollX / window.scrollY` 保持 `0`，页面没有被滚动带走。

## 2026-07-08：支持复制图片后直接粘贴到画布

### 需求收敛

- 用户希望复制图片后，可以直接在像素无限画布里粘贴。
- 推荐行为：剪贴板里有图片时，按 `Cmd/Ctrl + V` 自动像素化成一个新的画布对象，并放到当前视口中心；没有剪贴板图片时，保留原来的画布对象复制粘贴。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - 新增 `paste` 事件监听，优先读取剪贴板图片。
  - 粘贴图片复用现有 `createSpriteDocumentFromPng` 像素化管线，默认生成 `64x64`、`32` 色对象。
  - 粘贴对象位置改为当前画布视口中心，粘贴后自动选中新对象。
  - 没有剪贴板图片时，继续使用已有的画布对象复制粘贴逻辑。
  - 粘贴图片处理中，顶部导入按钮显示“粘贴中...”。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用 Codex 自带 Playwright CLI wrapper 验证：
  - 构造 PNG 剪贴板事件并派发到 `/workspace`。
  - 画布对象数量从 `0` 增加到 `1`。
  - 状态显示“已粘贴「粘贴图片-...png」为 64x64 像素对象”。
  - 右侧属性面板出现“像素分辨率”，说明粘贴对象已被选中且可继续编辑。
  - 再次粘贴后对象数量从 `1` 增加到 `2`。

## 2026-07-08：去掉像素对象外层容器留白

### 问题

- 用户截图反馈：像素图外还有一层容器，网格没有撑满图片区域。
- 根因：工作区对象 `.workspace-canvas-item` 有 `padding: 14px` 和 `3px` 边框，像素图、魔棒选区和像素网格都被放在内部内容区里，所以图片外会露出一圈棋盘格容器。

### 变更

- 更新 `src/app/workspace/page.tsx`：
  - `workspaceItemPadding` 从 `14` 改为 `0`，笔刷/橡皮/魔棒坐标按完整对象区域计算。
- 更新 `src/app/globals.css`：
  - `.workspace-canvas-item` 去掉基础边框、padding、棋盘背景和阴影。
  - 选中态改用外部 `outline` 和黄色外发光，不再占用图片内部空间。
  - `.workspace-pixel-selection` 和 `.workspace-pixel-grid` 从 `inset: 14px` 改为 `inset: 0`。

### 验证

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用 Codex 自带 Playwright CLI wrapper 验证：
  - 粘贴测试图片并显示网格。
  - 对象外框、图片、网格覆盖层均为 `256x256`。
  - 三者坐标一致，均为同一块区域。
  - 对象基础 `border` 为 `0px`，`padding` 为 `0px`，背景为 `none`。

## 2026-07-29：保存可靠性优化

### 目标

- 用户要求先保证编辑内容在刷新后仍能保存和恢复，避免刷新、关闭页面或存储异常时静默丢稿。

### 实现事实

- 更新 `src/app/workspace/page.tsx`：
  - 工作区完成初次读取后，后续变更会延迟自动保存；连续保存通过队列串行执行，避免较早的保存结果覆盖较新的内容。
  - 顶部操作区和状态栏会显示“等待自动保存 / 保存中 / 已保存 / 保存失败”。保存失败会提示浏览器存储空间或权限问题，并保留“重试保存”入口。
  - 恢复最近工作区失败或找不到记录时，不创建新的空白画布覆盖原有数据；页面改为报错并要求刷新重试。
  - 有尚未落盘的修改时，离开或刷新页面会触发浏览器的未保存提醒。
- 更新 `src/app/editor/[projectId]/page.tsx`：
  - 单张像素图编辑器采用同样的延迟自动保存、串行保存队列、保存状态和失败提示。
  - 项目读取失败时保留错误状态，不以空白文档替代读取结果。
  - 有未保存修改时离开或刷新页面会触发浏览器的未保存提醒。

### 说明

- 本次不改变像素绘制、图层、导出或动画功能；仅加强数据写入、恢复与用户可见反馈。
- 浏览器的离开提醒由浏览器决定最终展示形式，不能替代正常的自动保存。
