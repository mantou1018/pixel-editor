# 像素图生成器验证记录

## 2026-06-09：文字生成像素图工作流第一阶段

### 已通过

- `npm run build` 通过。
- 本地开发服务：`http://localhost:3000` 可启动。
- Playwright 打开 `http://localhost:3000/projects` 成功。
- 项目页可看到“文字生成像素图”区域。
- 点击“生成 4 个候选”后，页面出现 4 个候选卡片。
- 点击第一个“选中并编辑”后，成功跳转到 `/editor/<projectId>`。
- 编辑器读取到生成项目，显示 `64x64`、`生成图层`、`第 1 帧`。
- 编辑器右侧显示生成来源：`文字生成占位：可爱宠物：一只黄色小狗，正面，像素风`。
- 新增 `src/app/icon.svg` 后，重新打开项目页没有再出现 `favicon.ico` 404。

### 已知限制

- 当前未接真实 AI，生成候选是本地占位算法。
- 尚未做移动端真实截图验收。
- 尚未验证导出 PNG 在所有浏览器中的文件尺寸和透明背景表现。

## 2026-06-10：PNG 导出设置

### 已通过

- `npm run build` 通过。
- Playwright 重新验证生成候选进入编辑器流程通过。
- 编辑器右侧出现“导出设置”区域。
- 可切换导出尺寸：原始尺寸 / 预览大图。
- 可切换导出背景：透明背景 / 当前颜色背景。
- 选择“预览大图 + 当前颜色背景”后点击“导出 PNG”，下载文件名为 `一只黄色小狗，正面，像素风-1-512px-preview.png`。
- 该预览文件经 `file` / `sips` / `sharp` 验证为 `512 x 512`，透明像素 `0`，全部为不透明像素。
- 选择“原始尺寸 + 透明背景”后点击“导出 PNG”，下载文件名为 `一只黄色小狗，正面，像素风-1-64x64.png`。
- 该原始文件经 `file` / `sips` / `sharp` 验证为 `64 x 64`，透明像素 `2588`，不透明像素 `1508`。

### 已知限制

- 尚未做移动端真实截图验收。
- 尚未验证 Safari / Chrome 之外的下载行为。

## 2026-06-10：PNG 导入调色板归并

### 已通过

- `npm run build` 通过。
- Playwright 打开 `http://localhost:3000/projects` 成功。
- 项目页 PNG 导入区域显示 8 / 16 / 32 / 64 / 128 / 256 / 512 色调色板按钮。
- 点击 8 色、16 色后，选中态能正确切换。
- 使用临时测试 PNG `/tmp/pixel-import-palette-test.png` 走真实页面上传流程成功。
- 选择 16 色导入后，编辑器来源显示 `PNG 导入：pixel-import-palette-test.png，16 色`。
- 导出 JSON 后检查 `SpriteDocument`：
  - `palette.length` 为 16。
  - 当前帧实际像素颜色数为 16。
  - 来源记录为 `PNG 导入：pixel-import-palette-test.png，16 色`。

### 已知限制

- 文件上传验证使用 Playwright 和临时 PNG，尚未做人工视觉对比。
- 调色板归并使用最近色匹配，尚未加入专业像素画的抖动、边缘增强和手动调色板编辑。

## 2026-06-10：用户视角主流程测试

### 已通过

- `npm run build` 通过。
- Playwright 从 `http://localhost:3000/projects` 跑通：
  - 点击“生成 4 个候选”后出现 4 个候选。
  - 生成历史出现，并显示当前提示词、尺寸、风格和时间。
  - 点击第一个候选的“收藏”后，按钮变为“已收藏”。
  - 收藏候选区域出现，并显示缩略图、标题和“编辑此收藏”按钮。
  - 点击“编辑此收藏”后进入编辑器。
  - 编辑器显示生成来源、导出设置、图层和帧信息。
  - 点击“导出 PNG”后下载 `64x64` PNG。
  - 点击“← 存档”后回到项目存档页，项目卡片出现。

### 测试视角发现

- 已修：收藏夹按钮从“编辑”改为“编辑此收藏”，避免用户和测试都难以区分。
- 风险：生成历史和收藏目前不持久化，刷新页面会丢失。
- 风险：候选卡片中的多个“收藏”按钮同名，自动化测试需要按卡片上下文定位；真实用户可通过卡片位置区分。

## 2026-06-10：生成工作台持久化与用户视角复测

### 已通过

- `npm run build` 通过。
- Playwright 在 `http://localhost:3000/projects` 跑通同一浏览器会话：
  - 点击“生成 4 个候选”后出现 4 个候选。
  - 点击第一个候选“收藏”后，按钮变为“已收藏”，收藏候选区域出现。
  - 同页 `reload` 后，4 个候选、收藏候选、生成历史、提示词、尺寸和风格均恢复。
  - 点击“再次使用”后，状态提示为“已恢复历史候选，可继续选择或重新生成。”
  - 点击“编辑此收藏”后进入编辑器。
  - 点击“导出 PNG”后下载 `一只黄色小狗，正面，像素风-1-64x64.png`，编辑器状态显示 `已导出 PNG：64x64，透明背景`。
  - 点击“← 存档”后回到项目页，项目列表出现对应项目卡片。
- 移动端宽度 `390x844` 检查：
  - 项目页可访问新建、导入、生成、收藏、历史和项目列表。
  - `documentElement.clientWidth`、`documentElement.scrollWidth`、`body.scrollWidth` 均为 `390`，没有横向溢出。

### 已知限制

- 生成历史和收藏保存在本机浏览器 IndexedDB，不是云端账号同步。
- 这轮移动端只验证项目页，没有完整验证编辑器移动端画布操作。

## 2026-06-10：PNG 导入 512 色选项验证

### 已通过

- `npm run build` 通过。
- Playwright 打开 `http://localhost:3000/projects` 成功。
- PNG 导入区域显示 128 色、256 色、512 色按钮。
- 点击“512 色”后按钮进入选中态。

### 已知限制

- 这轮只验证选项出现和可选择，未重新上传一张高色数 PNG 做视觉对比。

## 2026-06-10：同级选项折叠交互验证

### 已通过

- `npm run build` 通过。
- Playwright 打开 `http://localhost:3000/projects` 成功。
- 默认状态下：
  - 尺寸只显示当前 `64x64` 和展开箭头。
  - PNG 导入调色板只显示当前 `32 色` 和展开箭头。
  - 生成风格只显示当前 `可爱宠物` 和展开箭头。
- 点击调色板箭头后显示其它颜色数，选择 `512 色` 后自动收起并显示 `512 色`。
- 点击尺寸箭头后显示其它尺寸，选择 `32x32` 后自动收起，并且生成说明同步显示 `32x32`。
- 点击生成风格箭头后显示其它风格，选择 `头像` 后自动收起。
- 移动端宽度 `390x844` 下 `clientWidth` 和 `scrollWidth` 均为 `390`，没有横向溢出。
- 选择折叠选项后点击“生成 4 个候选”，页面成功生成 4 个 `32x32` 头像候选。

## 2026-06-11：同级选项 PC 浮层下拉验证

### 已通过

- `npm run build` 通过。
- Playwright 打开 `http://localhost:3000/projects` 成功。
- 默认状态下三组同级选项仍只显示当前选项和箭头。
- 点击 PNG 导入颜色数箭头后，更多颜色数显示为悬浮弹窗；`选择 PNG 并转为网格` 按钮位置保持在同一行，没有被往下推。
- 点击生成风格箭头后，更多风格显示为悬浮弹窗；生成按钮位置保持不变。
- 移动端宽度 `390x844` 下 `clientWidth` 和 `scrollWidth` 均为 `390`，没有横向溢出。
- 选择“头像”后点击“生成 4 个候选”，页面成功生成 4 个 `64x64` 头像候选。

## 2026-06-11：taste-skill UI / 交互走查

### 已覆盖

- 项目页默认态截图。
- PNG 导入颜色数下拉截图。
- 生成风格下拉截图。
- 文字生成候选后的截图。
- 候选进入编辑器后的截图。
- 编辑器导出 PNG 流程。
- 编辑器移动端 `390x844` 截图。
- 项目页移动端 `390x844` 截图。

### 关键发现

- 项目页首屏焦点偏离“生图”，移动端首屏看不到生图入口。
- 生成风格下拉会遮挡“生成 4 个候选”，且 Esc 不关闭浮层。
- 编辑器默认 10x 缩放导致图像被裁切，缺少 fit-to-view。
- 移动端编辑器虽然无横向溢出，但实际编辑体验很差。
- 候选卡首屏只能看图，操作按钮容易被截断。
- 详细问题和给经理 / 研发的建议已记录到 `REVIEW.md`。

## 2026-06-11：REVIEW P0 交互优化复测

### 已通过

- `npm run build` 通过。
- Browser 插件连接成功；第一次新建标签因当时服务未监听报 `ERR_BLOCKED_BY_CLIENT`，重启 `npm run dev -- -p 3000` 后可打开 `http://localhost:3000/projects`。
- 桌面端 `1280x720`：
  - 项目页第一内容区为“文字生成像素图”，生成按钮在首屏内。
  - 生成风格下拉浮层位于触发器上方，不再遮挡“生成 4 个候选”。
  - 按 Esc 后风格下拉可关闭。
  - 在风格下拉打开后点击“生成 4 个候选”，浮层会收起，并成功生成 4 个候选。
  - 点击候选“选中并编辑”进入编辑器。
  - 编辑器首屏能完整看到 64x64 图，状态显示 `64x64 · 缩放 7x`。
  - 编辑器右侧首个面板为“导出设置”，顶部不再直接暴露“导出 JSON”。
  - 点击顶部“导出 PNG”后，状态显示 `已导出 PNG：64x64，透明背景`。
  - 控制台 `error` / `warn` 为空。
- 移动端 `390x844`：
  - 项目页 `clientWidth` 和 `scrollWidth` 均为 `390`，无横向溢出。
  - 首屏可看到文字生图入口和“生成 4 个候选”按钮。
  - 进入编辑器后 `clientWidth` 和 `scrollWidth` 均为 `390`，无横向溢出。
  - 编辑器首屏能完整看到 64x64 图，状态显示 `64x64 · 缩放 4x`。
  - 移动端编辑器首屏可见“导出 PNG”按钮。

### 已知限制

- 候选卡已压缩高度，但还没有增加“推荐 / 颜色更少 / 轮廓更清晰”等决策标签。
- PNG 上传区已增强上传感，但还没有做导入前文件预览。
- 移动端编辑器当前目标是可查看、可缩放、可导出；完整触控绘制体验仍需要单独设计。

## 2026-06-11：生图与已有素材左右布局验证

### 已通过

- `npm run build` 通过。
- `curl -I http://localhost:3000/projects` 返回 `HTTP/1.1 200 OK`。
- 桌面端 `1280x720`：
  - “文字生成像素图”和“已有素材编辑”位于同一行左右布局。
  - “文字生成像素图”在左侧，宽度约 `796px`。
  - “已有素材编辑”在右侧，宽度约 `362px`。
  - `clientWidth` 和 `scrollWidth` 均为 `1280`，无横向溢出。
- 移动端 `390x844`：
  - 两个模块自动上下堆叠。
  - `clientWidth` 和 `scrollWidth` 均为 `390`，无横向溢出。

## 2026-06-11：画布手动缩放不回弹验证

### 已通过

- `npm run build` 通过。
- 在编辑器中连续点击 `+` 6 次：
  - 初始状态：`64x64 · 缩放 4x · 适应窗口`。
  - 点击后状态：`64x64 · 缩放 10x · 手动缩放`。
  - 等待 1.5 秒后仍保持：`64x64 · 缩放 10x · 手动缩放`，未回弹。
  - 点击“适应”后恢复为：`64x64 · 缩放 4x · 适应窗口`。

### 结论

- 手动放大现在不会被自动 fit-to-view 覆盖。
- 只有用户主动点击“适应”时，画布才回到适应窗口大小。

## 2026-06-11：生成候选删除验证

### 已通过

- `npm run build` 通过。
- 候选卡片显示“删除”按钮。
- 点击第一个候选的“删除”后：
  - 候选数量从 `4` 变为 `3`。
  - 状态提示显示 `已删除候选：可爱宠物 方案 1`。
  - 页面刷新后候选数量仍为 `3`，删除结果已写入本机生成工作台。
- 删除候选会同步从收藏 ID 中移除，避免收藏区残留已删除候选。

### 附带修复

- 候选卡预览区域增加 `overflow: hidden`，图片改为在预览框内 `contain`，避免左右布局窄栏下图片压到标题和按钮。

## 2026-06-11：生成结果区布局修复验证

### 已通过

- `npm run build` 通过。
- 生成结果已从左侧生图面板移出，放到左右工作区下方的全宽“生成结果”区域。
- 桌面端 `1280x720`：
  - 左右工作区宽度约 `1163px`，生成结果区域同样约 `1163px`。
  - 候选卡固定为 `280px` 宽，一行最多容纳 4 个，不会因为只有 2 个或 3 个候选就被拉宽。
  - 每张卡的 `收藏 / 删除 / 选中并编辑` 三个按钮均完整显示在卡片内。
  - `clientWidth` 为 `1265`，`scrollWidth` 为 `1265`，无横向溢出。
  - 当前 3 个候选实测宽度均为 `280px`，左对齐排列，生成结果容器最大宽度为 `1168px`。

### 结论

- 截图中“卡片互相压住、按钮被裁切”和“候选少时卡片被撑得过宽”的问题已修复。

## 2026-07-05：像素无限画布工作区地基验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/projects` 返回 `HTTP/1.1 200 OK`。
- 代码检查确认：
  - 项目页出现 `128x128`、`256x256` 尺寸文案。
  - 图片导入入口已改为 PNG / JPG / WebP。
  - 新增 `CanvasWorkspace`、`WorkspaceItem` 数据结构。
  - IndexedDB 新增 `workspace_summaries` 和 `workspace_documents`。

### 未验证

- 未做浏览器截图验证，因为本项目当前没有 Playwright 依赖，本轮不新增依赖。
- 未验证真实 JPG / WebP 文件导入。
- 未验证 Workspace UI，因为本轮只完成数据层和保存层，还没有无限画布页面。

### 结论

- 工作区地基代码可通过类型检查和生产构建。
- 当前页面可正常响应。
- 下一轮可以进入无限画布 UI 和多对象导入实现。

## 2026-07-06：像素无限画布页面验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 构建路由列表包含 `/workspace`。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。

### 未验证

- 未做 Playwright 截图验证，因为当前环境缺少 Playwright 浏览器二进制，本轮不额外安装。
- 未用真实图片手动验证导入、重新像素化和导出。

### 结论

- `/workspace` 已经是可访问的新像素无限画布页面。
- `/projects` 仍保留旧项目存档页，但顶部新增了进入像素无限画布的入口。

## 2026-07-06：画布查看缩放验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 打开 `http://localhost:3000/workspace`：
  - 页面标题显示“像素无限画布”。
  - 画布右上角显示缩放控件：`- / 100% / + / 适应`。
  - 初始状态显示“画布已就绪。导入图片后会自动像素化为独立对象。”

### 未验证

- 未用真实图片验证缩放后拖动对象的手感。
- 未验证触控板多指手势；当前只实现 `Cmd/Ctrl + 滚轮` 查看缩放。

### 结论

- 画布已有基础查看缩放能力，可以继续补 Figma 式平移和快捷键。

## 2026-07-06：工作区一屏自适应验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 以 `1280x800` 打开 `http://localhost:3000/workspace`：
  - `window.innerHeight` 为 `800`。
  - `document.body.scrollHeight` 为 `800`。
  - `document.documentElement.scrollHeight` 为 `800`。
  - `.workspace-page` 高度为 `800`。
  - 页面整体不再产生上下滚动。

### 结论

- `/workspace` 已改为撑满一屏。后续溢出内容会在左侧对象列表、画布 viewport 或右侧参数面板内部滚动。

## 2026-07-06：Figma 式画布布局和导航验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 以 `1280x800` 打开 `http://localhost:3000/workspace`：
  - 顶部为紧凑工具栏，显示“选择 / 抓手”。
  - 中央画布区域为主视觉，左侧为对象列表，右侧为属性面板。
  - 画布右上角显示缩放控件：`- / 100% / + / 适应`。
  - `document.body.scrollHeight` 与 `window.innerHeight` 均为 `800`，页面整体不滚动。
  - 按 `H` 后抓手工具选中。
  - 按住空格后画布 viewport 进入 `hand-active` 状态。
  - 按 `V` 后选择工具选中。

### 未验证

- 未用真实对象验证 `Shift + 1` 适应全部和 `Shift + 2` 适应选中后的视觉位置。
- 未验证触控板上的平移/缩放手感。

### 结论

- `/workspace` 的布局和基础导航已经更接近 Figma 式画布工作台。

## 2026-07-06：Figma 核心画布交互第一批验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 导入 2 张测试图片后，画布出现 2 个对象。
  - `Shift + 点击` 后 2 个对象均进入选中态。
  - `Cmd + C`、`Cmd + V` 后对象数从 2 变为 4。
  - `Delete` 后对象数从 4 回到 2。
  - 多选后左侧显示选中数量。

### 未验证

- 未手动验证框选矩形在复杂对象布局下的边界命中。
- 未验证 `Cmd/Ctrl` 在 Windows 键盘布局下的全部表现。
- 未验证大量对象场景下的性能。

### 结论

- `/workspace` 已补齐第一批 Figma 式画布核心交互：多选、复制粘贴、删除、方向键移动和框选基础。

## 2026-07-06：大范围画布缩放验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用系统 Chrome 自动验证：
  - 连续点击缩小后，缩放比例到达 `2.0%`。
  - 连续点击放大后，缩放比例到达 `6400%`。
  - 按 `Cmd + 1` 后，缩放比例回到 `100%`。

### 结论

- `/workspace` 已支持接近无限画布所需的大范围查看缩放。

## 2026-07-06：画布区域妙控板缩放验证

### 已通过

- `npm run build` 通过。
- build 后单独执行 `npx tsc --noEmit` 通过。
- 使用系统 Chrome 自动验证：
  - 鼠标位于 `.canvas-viewport` 内。
  - 按住 Control 并滚轮后，画布缩放从 `100%` 变为 `112%`。
  - `window.visualViewport.scale` 保持 `1`，网页没有整体缩放。

### 注意

- 曾并行执行 `npx tsc --noEmit` 与 `npm run build`，`tsc` 命中过一次 `.next/types` 生成文件竞态；按项目验证规则，build 后单独重跑 `tsc` 已通过。

### 结论

- 画布区域已接管妙控板/触控板式缩放，避免浏览器页面整体缩放。

## 2026-07-06：低倍缩放白块修复验证

### 已通过

- `npm run build` 通过。
- build 后单独执行 `npx tsc --noEmit` 通过。
- 使用系统 Chrome 自动验证：
  - 打开 `http://localhost:3000/workspace` 后，页面整体高度等于视口高度，没有上下滚动。
  - 连续点击缩小到 `2.0%`。
  - 原截图中左上角白块区域命中的元素为 `.canvas-viewport`。
  - `.canvas-grid-surface` 的背景为 `none`，说明有限舞台不再绘制白底。
  - `.canvas-viewport` 的网格背景尺寸为 `8px 8px`，低倍缩放时网格仍可见且不会密到发灰。
  - 鼠标位于画布区域内，按住 Control 滚轮后，画布缩放从 `100%` 变为 `272%`。
  - `window.visualViewport.scale` 保持 `1`，浏览器页面没有整体缩放。

### 结论

- 截图中的低倍缩放白块已修复。
- 当前缩放逻辑已收敛为：按钮/快捷键使用固定缩放档位，触控板/滚轮使用连续缩放，并统一限制在 `2% - 6400%`。

## 2026-07-07：当前像素对象内编辑工具验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 本项目原 3000 dev server 曾出现 `write EPIPE` 后不响应；确认 PID 属于 `/Users/mantou/我的/项目/像素图生成器` 后，精确重启该项目 dev server，`http://localhost:3000/workspace` 恢复可访问。
- 使用系统 Chrome 自动验证：
  - 上传截图文件后，画布出现 1 个 `.workspace-canvas-item`，并处于选中态。
  - 顶部工具栏包含“笔刷”，右侧面板包含“对象内编辑”。
  - 点击笔刷，设置颜色为 `#ff0000`，在对象内部拖动画笔后，右侧预览图 data URL 发生变化。
  - 笔刷绘制后状态为“已绘制像素”。
  - 点击吸管并点选对象内像素后，取色值变为 `#202020`，工具自动切回“笔刷”。
  - 点击魔棒并点选对象后，页面生成 1702 个 `.workspace-pixel-selection i` 选区格，并显示“清除魔棒选区”。
  - 按 `Cmd + Z` 后状态显示“已撤销上一步编辑”。

### 未验证

- 未在触控板真实手势下验证长时间连续绘制手感。
- 未验证大尺寸 `256x256` 对象上魔棒选区覆盖大量像素时的性能上限。
- 未验证 Windows 键盘下 `Ctrl + Z / Ctrl + Shift + Z`。

### 结论

- `/workspace` 已具备第一版 PS 基础像素编辑能力：笔刷、橡皮、吸管、填充、魔棒选择、撤销和重做。
- 当前实现限定在“当前选中的像素对象”内部，不会直接在整个无限画布上涂鸦。

## 2026-07-07：画布缩放丝滑化验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。
- 使用系统 Chrome 自动验证：
  - 初始缩放显示 `100%`，CSS 变量 `--canvas-scale` 为 `1`。
  - 鼠标位于 `.canvas-viewport` 内，按住 Control 连续 8 次小幅 wheel。
  - 缩放显示变为 `119%`，CSS 变量 `--canvas-scale` 为 `1.1886`。
  - `window.visualViewport.scale` 保持 `1`。
  - `window.scrollX / window.scrollY` 保持 `0`。
  - 点击缩放 `+` 后显示 `150%`。
  - `.canvas-grid-surface` 的过渡为 `transform 0.09s ease-out`。

### 结论

- 画布内滚轮 / 触控板缩放已改为更细粒度的连续缩放。
- 按钮缩放已有短过渡，视觉上不再是硬切。

## 2026-07-07：魔棒选区切换工具保持验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。
- 使用系统 Chrome 自动验证：
  - 上传图片并选中像素对象。
  - 点击魔棒后，在对象内部点选区域，页面生成 1703 个 `.workspace-pixel-selection i`。
  - 点击橡皮后，选区仍为 1703 个，并继续显示“清除魔棒选区”。
  - 点击笔刷后，选区仍为 1703 个，并继续显示“清除魔棒选区”。

### 结论

- 魔棒选区现在会在笔刷、橡皮、吸管、填充、魔棒之间保留。
- 切回选择或抓手时仍会清除选区，表示离开对象内部编辑。

## 2026-07-07：笔刷和橡皮跟手感与工具光标验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。
- 使用系统 Chrome 自动验证：
  - 上传图片并选中像素对象。
  - 点击笔刷后，`.workspace-canvas-item` 的 cursor 为内嵌笔刷 SVG URL。
  - 在对象内部用较少 pointer steps 快速斜向拖动画笔后，右侧预览 data URL 发生变化，状态显示“已绘制像素”。
  - 点击橡皮后，`.workspace-canvas-item` 的 cursor 为内嵌橡皮 SVG URL，当前工具为“橡皮”。

### 结论

- 笔刷和橡皮拖动时已用线段插值补齐中间像素，减少快速拖动时的漏格。
- 笔刷和橡皮在当前选中像素对象上已有对应工具光标。

## 2026-07-07：画笔尖和像素落点对齐验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。
- 使用系统 Chrome 自动验证：
  - 上传图片并选中像素对象。
  - 对象外框尺寸为 `256x256`。
  - 对象内图片内容区尺寸为 `222x222`。
  - 图片内容区相对对象外框存在约 `17px` 内缩。
  - 点击图片内容区中心后，预览 data URL 发生变化，状态显示“已绘制像素”。
  - 笔刷 cursor 显示内嵌 SVG，热点为 `3 25`。
  - 橡皮 cursor 显示内嵌 SVG，热点为 `13 17`。

### 结论

- 笔刷和橡皮的像素坐标现在按真实图片内容区计算，不再按包含 padding 的对象外框计算。
- 画笔尖与实际落点的偏移已修正。

## 2026-07-07：魔棒选区外轮廓验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。
- 使用系统 Chrome 自动验证：
  - 上传图片并选中像素对象。
  - 点击魔棒并在对象内部点选区域后，页面生成 1703 个 `.workspace-pixel-selection i`。
  - 第一个选区格 `outline-style` 为 `none`。
  - 选区格不再统一画完整 outline，而是根据相邻关系生成四方向 border。
  - 前 30 个选区格中出现 `0px 0px 0px 0px` 的内部像素，说明内部相邻边不画线。
  - 前 30 个选区格中也出现单侧边框模式，例如 `0px 2px 0px 0px`、`2px 0px 0px 0px`，说明只画外轮廓边。

### 结论

- 魔棒选区不再以每个像素完整方框显示，内部交叉线已移除。
- 当前视觉是淡蓝覆盖 + 外轮廓边线，更接近图像编辑软件里的选区遮罩。

## 2026-07-08：工作区对象背景套娃验证

### 已通过

- `npm run build` 通过。
- build 后单独执行 `npx tsc --noEmit` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。
- 使用系统 Chrome 自动验证：
  - 上传截图并选中像素对象。
  - `.workspace-canvas-item img` 的 src 为 PNG data URL。
  - 缩略图尺寸为 `256x256`。
  - 采样到透明像素，RGBA 为 `[0, 0, 0, 0]`。
  - 外层 `.workspace-canvas-item` 的背景仍为棋盘格渐变。

### 注意

- 第一次并行执行 `npx tsc --noEmit` 和 `npm run build` 时，`tsc` 命中过 `.next/types` 生成文件竞态；`npm run build` 通过后单独重跑 `tsc` 已通过。

### 结论

- `/workspace` 内部缩略图不再自带棋盘格，透明区域由外层对象容器统一显示。
- 背景套娃问题已修复。

## 2026-07-08：清空选区内容和浅灰像素网格验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。
- 使用系统 Chrome 自动验证：
  - 上传图片并选中像素对象。
  - 点击“显示网格”后，页面出现 `.workspace-pixel-grid`。
  - 网格背景为浅灰色：`rgba(156, 163, 175, 0.42)`。
  - 网格尺寸随当前对象像素分辨率生成，本次为 `1.5625% 1.5625%`。
  - 点击魔棒并点选区域后，生成 2707 个选区像素。
  - 页面出现“清空选区内容”按钮。
  - 点击“清空选区内容”后，右侧预览 data URL 发生变化。
  - 状态显示“已清空选区内容：2707 个像素”。
  - 清空后选区仍保留，网格仍显示。
  - 点击“隐藏网格”后，`.workspace-pixel-grid` 消失，按钮恢复为“显示网格”。

### 结论

- 当前魔棒选区可以一键清空为透明像素。
- 选中对象支持浅灰像素网格显示 / 隐藏。

## 2026-07-08：触控板缩放灵敏度验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- `curl -I http://localhost:3000/workspace` 返回 `HTTP/1.1 200 OK`。
- 代码检查确认 `wheelZoomSensitivity` 已从 `0.0012` 提高到 `0.0018`。
- 缩放仍走原有 `Math.exp(-deltaY * wheelZoomSensitivity)` 连续缩放公式，仍复用同一套 `2% - 6400%` 上下限。
- 使用 Codex 自带 Playwright CLI wrapper 验证：
  - 初始缩放显示 `100%`，CSS 变量 `--canvas-scale` 为 `1`。
  - 鼠标位于 `.canvas-viewport` 中心，按住 Control 后连续 8 次小幅 wheel。
  - 缩放显示变为 `77%`，CSS 变量 `--canvas-scale` 为 `0.7717`。
  - `window.visualViewport.scale` 保持 `1`，浏览器页面没有整体缩放。
  - `window.scrollX / window.scrollY` 保持 `0`，页面没有被滚动带走。

### 未验证

- 未在真实妙控板硬件上长时间试手感。

### 结论

- 画布区触控板 / 滚轮缩放响应已提高约 50%。
- 按钮缩放档位、缩放上下限和防止浏览器页面整体缩放的逻辑未改动。

## 2026-07-08：剪贴板图片粘贴验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用 Codex 自带 Playwright CLI wrapper 打开 `http://localhost:3000/workspace`。
- 构造 PNG 图片 `ClipboardEvent` 后派发：
  - 第一次粘贴后，`.workspace-canvas-item` 数量从 `0` 变为 `1`。
  - 状态显示“已粘贴「粘贴图片-...png」为 64x64 像素对象”。
  - 页面包含“像素分辨率”，说明右侧属性面板已进入单对象编辑状态。
  - 第二次粘贴后，`.workspace-canvas-item` 数量从 `1` 变为 `2`。
- Playwright console 检查无 warning。

### 未验证

- 未读取真实系统剪贴板内容；自动验证使用浏览器内构造的 PNG 剪贴板事件。
- 未在 Safari 上验证图片剪贴板兼容性。

### 结论

- `/workspace` 已支持剪贴板图片粘贴为新的像素对象。
- 无图片剪贴板时，原有画布对象复制粘贴逻辑保留。

## 2026-07-08：像素对象容器留白移除验证

### 已通过

- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 使用 Codex 自带 Playwright CLI wrapper 打开 `http://localhost:3000/workspace`。
- 粘贴测试图片并点击“显示网格”后检查尺寸：
  - `.workspace-canvas-item` 为 `256x256`。
  - `.workspace-canvas-item img` 为 `256x256`。
  - `.workspace-pixel-grid` 为 `256x256`。
  - 三者坐标一致。
  - 对象基础 `border` 为 `0px`。
  - 对象 `padding` 为 `0px`。
  - 对象背景为 `none`。
  - 网格 `inset` 为 `0px`。

### 结论

- 像素图外层棋盘格容器留白已移除。
- 图片、像素网格、魔棒选区和编辑命中区域现在对齐同一个对象边界。

## 2026-07-29：保存可靠性验证

### 已通过

- `npm run build` 通过。
- `curl -I http://localhost:3001/workspace` 返回 `HTTP/1.1 200 OK`。
- 代码检查确认工作区与单图编辑器均具备：延迟自动保存、串行保存队列、保存状态显示、保存失败提示、读取失败不创建空白内容，以及未保存离开提醒。
- 在 `http://localhost:3001/workspace` 的独立自动化浏览器会话完成工作区闭环：
  - 保存一个 `0` 对象工作区。
  - 刷新后状态显示“已恢复工作区：0 个对象”。
  - 再次点击保存后状态显示“已保存工作区：0 个对象”。
  - 浏览器控制台 `error` 和 `warn` 均为空。

### 过程说明与剩余验证

- 曾尝试使用 `3002` 隔离端口验证，但因已有 `next dev` 进程占用而被拒绝；该过程不影响最终结论。
- 已通过真实浏览器写入、刷新和再次保存验证，工作区保存恢复闭环成立。
- 尚未验证包含实际像素对象的“大文件保存 / 存储空间不足后重试”场景；这是上线前的补充风险项，不影响本次空工作区刷新恢复结论。
