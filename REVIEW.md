# 像素图生成器体验评审记录

## 2026-06-11：taste-skill 高标准 UI / 交互走查

### 评审口径

- 评审角色：设计师视角，按高标准 UI 设计师和交互设计师要求检查。
- 使用方法：按 `taste-skill` 的目标拆成视觉密度、层级与焦点、PC 控件范式、用户任务闭环四类。
- 覆盖路径：
  - 项目页默认态。
  - 尺寸、颜色数、生成风格下拉。
  - 文字生成 4 个候选。
  - 候选进入编辑器。
  - 编辑器导出 PNG。
  - 项目页和编辑器移动端宽度检查。

### 总体判断

当前版本功能基本能跑通，但还不能算“交互满意”的像素生图软件。最大问题不是某个按钮样式，而是页面把“项目存档 / 新建 / PNG 导入 / 文字生图 / 编辑器开发态能力”混在同一层级里，导致主任务焦点不清。视觉语言也过于平均用力：大边框、大阴影、大按钮到处都是，缺少主次、密度和呼吸感。

### 问题标注索引

| 页面位置 | 严重度 | 主要问题 | 影响对象 | 建议负责人 |
| --- | --- | --- | --- | --- |
| 项目页首屏 | P0 | 生图入口不是第一视觉焦点，移动端首屏看不到生图 | 新用户、经理 | 经理先定信息架构，研发重排 |
| 生成风格下拉 | P0 | 浮层遮挡生成按钮，Esc 不关闭 | 所有桌面端用户 | 研发修控件行为 |
| 编辑器画布 | P0 | 默认 10x 导致图像裁切，缺少完整预览 | 编辑用户 | 研发做 fit-to-view |
| 移动端编辑器 | P0 | 桌面布局压成单列，真实编辑不可用 | 手机用户 | 经理定移动目标，研发重做布局 |
| 候选结果区 | P1 | 卡片太高，动作按钮不够及时可见 | 生图用户 | 设计定卡片结构，研发实现 |
| 顶部和右侧面板 | P1 | JSON、动作数、帧数等开发信息权重过高 | 普通用户 | 经理定默认暴露范围，研发下沉 |
| PNG 导入区 | P1 | 上传入口像普通按钮，缺少文件预览和导入确认 | 导入用户 | 设计补上传交互，研发实现 |

### P0：必须优先修

1. 项目页首屏焦点错位
   - 现象：桌面端首屏先看到项目存档、新建、PNG 导入，文字生图在下方；移动端首屏完全看不到生图入口。
   - 影响：产品叫“像素图生成器”，但用户第一眼不是“输入提示词生成”，而是“项目管理工具”。
   - 给经理：需要重新确认当前产品核心是否是“生图 + 编辑”。如果是，项目页应改成生图工作台，项目存档和 PNG 导入都降级。
   - 给研发：优先重排页面信息架构，而不是继续局部修按钮。

2. 生成风格下拉会遮挡生成按钮
   - 现象：生成风格浮层展开后覆盖“生成 4 个候选”，点击生成会被浮层拦截。
   - 影响：PC 下拉控件必须支持不阻断主操作，当前属于明显交互 bug。
   - 给研发：下拉应支持点击外部关闭、Esc 关闭；浮层位置应避开主要 CTA，或在点击其它按钮时自动关闭。

3. 编辑器默认画布视野不可控
   - 现象：64x64 图以 10x 缩放显示，桌面端主体被裁切；移动端只看到画布一角。
   - 影响：用户进入编辑器后无法完整判断图像，只能看到局部，不符合“编辑前先看整体”的基本需求。
   - 给研发：默认应 fit-to-view，提供缩放控件；不要固定 10x 作为默认。

4. 移动端编辑器不可用
   - 现象：顶部元信息和按钮占据大量首屏，画布被挤到下方且裁切；颜色控件过小；工具尺寸按钮只有三角符号。
   - 影响：虽然没有横向溢出，但真实编辑效率很差。
   - 给经理：移动端要么明确只做查看/导出，要么重做移动编辑布局。
   - 给研发：不能把桌面编辑器简单堆成单列。

### P1：显著影响专业感和效率

1. 所有模块视觉权重过于平均
   - 现象：顶部、项目开始区、生图区、候选卡、编辑器面板都使用相似的粗边框和阴影。
   - 影响：用户很难判断什么是主操作、什么是辅助信息。
   - 建议：只给主工作区保留强边框；辅助信息减弱为轻分隔或小标签。

2. 文案仍偏内部实现说明
   - 例子：`先用本地占位生成跑通工作流`、`文字生成占位`、`下一轮接真实 AI 或局部重绘`。
   - 影响：用户不需要看到研发阶段语言，会降低产品可信度。
   - 建议：改成面向用户的状态，例如“根据描述生成候选，进入编辑器后可手动修色”。

3. 候选卡缺少决策辅助
   - 现象：4 个候选只叫方案 1-4，没有差异标签、推荐标记、局部差异提示。
   - 影响：用户只能盲选。
   - 建议：为候选增加“更圆润 / 轮廓更清晰 / 颜色更少 / 推荐”等标签。

4. 候选卡首屏动作不可见
   - 现象：生成后截图里主要看到图，标题和按钮在首屏下方被截断。
   - 影响：用户看到结果后不知道下一步是否收藏、编辑、再生成。
   - 建议：候选卡减高，动作固定在卡片底部且首屏可见；或把主操作放到图下方即时可见。

5. PNG 导入入口像普通按钮，不像上传控件
   - 现象：`选择 PNG 并转为网格` 与普通按钮同形，没有拖拽区、文件名、预览、导入前确认。
   - 影响：用户对“选文件后会发生什么”缺少控制感。
   - 建议：改为上传区，显示支持格式、当前颜色数、导入后预览。

6. 编辑器右侧面板信息噪音过多
   - 现象：生成来源、控制、导出设置、图层同时堆叠；`动作数 / 帧数 / 图层` 对当前核心任务不是同等重要。
   - 影响：右侧面板像开发调试台，不像创作工具。
   - 建议：把右侧改成“导出 / 图层 / 信息”分组或 Tabs，默认只露出导出和当前图层。

7. 顶部操作过多且主次不清
   - 现象：撤回、保存、导出 JSON、导出 PNG 平铺；JSON 对普通用户不应是顶级按钮。
   - 影响：核心导出 PNG 被弱化。
   - 建议：主按钮只保留保存、导出 PNG；JSON 放进更多菜单或开发选项。

### P2：后续打磨

1. 英文小标题 `Sprite Archive` 和中文主标题混用，语义也不准。
   - 建议：如果当前页面是生成器，eyebrow 改成“Pixel Sprite Studio”或直接中文。

2. `第01/01页` 暂无真实分页意义。
   - 建议：没有分页时移除，避免假功能感。

3. 下拉浮层视觉仍粗糙。
   - 建议：浮层边框和阴影降噪，选项行增加 hover，浮层宽度贴合触发器或内容，不要像硬框。

4. 状态提示位置偏弱。
   - 建议：生成中、已生成、导出成功等状态靠近触发操作出现，而不是统一放在页面中后部。

5. 缺少空态示例图。
   - 建议：生图区域可以默认展示 1 个示例 sprite 或最近结果，降低空白感。

### 给经理的收口建议

建议下一阶段不要继续补零散功能，先做一次信息架构收敛：

1. 确认主页面是否只服务“文字生图”。
2. 如果是，把“新建空白”和“PNG 导入”放到次级入口。
3. 把项目存档从首屏主角降级为“最近编辑 / 打开已有项目”。
4. 编辑器分成桌面完整编辑和移动端轻量查看两套目标，不要默认两端同构。

### 给研发的执行建议

1. 先修下拉控件基本行为：外部点击关闭、Esc 关闭、避免遮挡主 CTA。
2. 项目页重排：生图区上移成为首屏核心，辅助入口降级。
3. 编辑器默认 fit-to-view，补缩放控件。
4. 顶部按钮瘦身：JSON 下沉，PNG 导出强化。
5. 移动端编辑器重新布局或限制为查看/导出。
