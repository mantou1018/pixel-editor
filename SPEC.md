# 精灵图制作工具 V1 规格

## 1. 产品定位

本项目 V1 的定位是：**做精灵图的工具**。

它将 **Aseprite 的像素编辑能力** 与 **Rika AI 的 AI 生成 / 导入工作流** 结合，帮助用户生成、导入、像素化、编辑和导出精灵图。

可以理解为：

- 入口：用户可以 AI 生成精灵图、导入现成精灵图，也可以导入任意普通图片并自动像素化为精灵图。
- 核心：所有输入都会变成一个个放在画布里的像素块，每个像素块都可以单独编辑。
- 编辑：像 Aseprite 一样，允许用户对精灵图进行基础逐像素编辑、帧管理和导出。

V1 不追求完整复刻 Aseprite，也不追求复杂 AI 工作流，而是先完成一个可用的最小闭环：

> 生成或导入图片 -> 自动像素化 / 像素块化 -> 进入像素编辑器 -> 选择 16x16 / 32x32 / 64x64 尺寸编辑 SpriteDocument -> 管理动作和帧 -> 导出 PNG / sprite sheet / JSON。

一句话定位：

> 不管图片来自 AI、现成精灵图，还是普通照片，系统都能把它转成可逐块编辑的精灵图。

## 2. V1 核心目标

V1 的目标是验证一个基础精灵图制作工具是否成立：

1. 用户可以通过生成、导入现成精灵图、导入普通图片三种入口创建一个精灵图项目。
2. 系统以 `SpriteDocument` 作为核心数据结构承载素材。
3. V1 支持 `16x16`、`32x32`、`64x64` 三种主尺寸。
4. 系统可以把输入图片像素化，并按像素块放到画布里。
5. 用户可以在画布上进行逐像素块编辑。
6. 用户可以管理精灵图动作和帧，V1 支持多个动作类型，每个动作最多 12 帧。
7. 用户可以导出单张 PNG、sprite sheet 和 JSON 数据。

## 3. 核心概念

### 3.1 SpriteDocument

`SpriteDocument` 是 V1 的核心文件 / 数据概念，用来描述一个精灵图项目。

它至少应包含：

- 基础信息：名称、尺寸、创建时间或更新时间。
- 主尺寸：支持 `16x16`、`32x32`、`64x64`。
- 帧数据：每个动作最多支持 12 帧。
- 动作类型：V1 支持多个动作类型，例如 idle、walk、run、attack、jump、hurt。
- 像素数据：用于逐像素编辑和导出。
- AI 来源：记录生成提示词、参考图或导入来源。
- 导出信息：用于生成 PNG、sprite sheet、JSON。

关系可以这样理解：

```text
SpriteDocument = 一个角色素材工程
Animation = 一个动作，例如 idle / walk / attack
Frame = 动作里的某一帧
PixelGrid = 某一帧里的像素格子数据
```

V1 可以先不开放复杂图层，但数据结构需要避免以后从单图扩展到动画时大返工。

### 3.2 主尺寸：16x16 / 32x32 / 64x64

V1 支持三种主工作尺寸：

- `16x16`：适合小图标、极简精灵和低成本测试。
- `32x32`：适合普通小型精灵，编辑负担较低。
- `64x64`：适合更完整的角色精灵和动作帧，是 V1 推荐默认尺寸。

创建空白文档、导入现成精灵图、普通图片像素化、AI 生成入口都需要允许用户选择目标尺寸。后续可以扩展到 `128x128`。

### 3.3 动画帧

V1 的动画能力保持轻量，但不限制为单一 `idle` 动作。

需要支持：

- 多个动作类型，例如 `idle`、`walk`、`run`、`attack`、`jump`、`hurt`
- 每个动作最多 12 帧
- 帧列表：允许用户查看、选择、复制或编辑不同帧
- 简单播放预览：用于判断动作是否顺眼

V1 暂不做完整专业时间轴、骨骼动画或自动补间。第一轮最小闭环可以先用一个默认动作跑通，但数据结构和 UI 不能写死为 `idle`。

### 3.4 图片像素化与像素块化

图片像素化是 V1 的核心能力之一。

系统需要支持三类输入：

- AI 生成的图片。
- 用户已有的像素图。
- 用户上传的普通图片。

无论图片来自哪一种入口，进入编辑器前都需要转换为 `SpriteDocument`：

```text
原始图片
-> 按目标尺寸采样，例如 16x16 / 32x32 / 64x64
-> 归并颜色，减少杂色
-> 生成 PixelGrid
-> 写入 SpriteDocument
-> 在画布中显示为可编辑像素块
```

这里的关键不是“显示一张图片”，而是把图片变成一组可编辑的数据。画布上的每个像素块都应该对应 `SpriteDocument` 里的一个数据单元。

## 4. V1 功能范围

### 4.1 生成 / 导入 / 像素化入口

V1 需要提供清楚的开始入口，让用户可以快速创建可编辑精灵图。

入口至少包括：

- AI 生成入口：用户通过描述生成像素角色素材。
- 现成精灵图导入入口：用户可以导入已有 PNG 精灵图，并转成可编辑像素块。
- 普通图片像素化入口：用户可以导入任意普通图片，系统自动缩小采样、归并颜色并转成精灵图。
- 创建空白文档入口：用户可以选择 `16x16`、`32x32`、`64x64` 创建空白 `SpriteDocument`。

V1 入口重点不是复杂参数，而是让用户能清楚开始：

> 我想生成一个角色 / 我已有像素图要编辑 / 我想把普通图片变成像素图 / 我想从空白画布开始。

### 4.2 轻量像素编辑器

V1 编辑器采用 Aseprite 式基础编辑体验，但只实现最小必要能力。

需要支持：

- 16x16 / 32x32 / 64x64 像素画布展示。
- 网格化编辑区域。
- 逐像素绘制。
- 单个像素块选择和改色。
- 橡皮擦或清除像素。
- 基础颜色选择。
- 当前帧编辑。
- 编辑结果实时写入 `SpriteDocument`。

建议界面结构：

- 左侧：生成 / 导入区、参考图、生成历史。
- 中间：像素画布，是视觉和操作中心。
- 右侧：当前颜色、基础调色板、当前帧信息。
- 底部：动作选择和最多 12 帧的简单帧列表。
- 顶部：保存、撤销、重做、导出、缩放、预览。

### 4.3 帧列表

V1 需要一个简单动作 / 帧列表，用来管理精灵图动作和每个动作最多 12 帧。

至少支持：

- 查看和切换动作。
- 查看当前帧列表。
- 选择某一帧进行编辑。
- 新增帧，每个动作最多 12 帧。
- 删除帧，但至少保留 1 帧。
- 复制当前帧作为新帧。
- 简单播放预览。

### 4.4 导出

V1 需要提供基础导出能力。

至少支持：

- 导出当前帧为 PNG。
- 导出所有帧为 sprite sheet PNG。
- 导出 `SpriteDocument` 对应的 JSON。

JSON 应能表达：

- 文档名称。
- 画布尺寸。
- 动作名。
- 帧数量。
- 每一帧的像素数据。
- sprite sheet 的基础切分信息。

## 5. UI 结构与视觉参考

V1 产品分为两个主页面：

- 编辑器：用于生成、导入、像素化、逐像素编辑、帧编辑、预览和导出。
- 项目存档：用于查看、筛选、管理和重新打开已有像素精灵项目。

视觉方向参考用户提供的两张截图，固定为：**深色紫色像素风精灵图编辑器**。

### 5.1 全局视觉规则

- 使用深紫黑作为主背景，面板使用更浅的紫黑或紫灰。
- 操作高亮使用亮紫、蓝紫或青紫，但不要大面积发光。
- 面板、按钮、卡片使用硬边框和像素风阴影，少用圆润玻璃感。
- 信息密度可以偏高，但必须通过标题、边框和间距清晰分区。
- 重要状态使用固定颜色，例如成功、生成中、草稿、收藏。
- 重要操作不要只用图标，必须提供中文文字或悬浮提示。

### 5.2 编辑器页面结构

编辑器参考截图 1，采用：

```text
顶部状态栏
左侧工具栏 + 中央画布 + 右侧控制面板
底部时间轴 / 帧列表
```

#### 顶部状态栏

用于显示当前项目和全局操作：

- 返回项目存档。
- 项目名或项目编号。
- 当前动作，例如 `idle`。
- 当前尺寸，例如 `16x16`、`32x32`、`64x64`。
- 当前帧数。
- 保存状态。
- 导出入口。

#### 左侧工具栏

用于放最核心编辑工具：

- 选择。
- 画笔。
- 橡皮。
- 吸管。
- 填充。
- 移动画布。
- 缩放。

工具栏图标需要足够大，选中态必须明显。用户不懂图标时，需要有悬浮提示或文字说明。

#### 中央画布

中央画布是编辑器的视觉中心，不能被 AI 面板或参数面板抢走。

画布需要支持：

- 像素网格。
- 透明背景棋盘格。
- 单个像素块选中高亮。
- 缩放比例显示。
- 拖动画布。
- 当前帧内容显示。
- 播放预览时隐藏或弱化网格。

#### 右侧控制面板

右侧控制面板参考截图 1 的“智能行动 / 历史 / 控制”结构，但 V1 需要简化。

建议分为三个折叠区：

- 智能行动：生成当前帧、生成动作帧、重新像素化、统一风格。
- 历史记录：撤销、重做、生成版本或最近操作。
- 控制面板：当前颜色、基础调色板、当前帧参数、导出设置。

V1 默认展开最常用的控制区，其余区域可以折叠，避免右侧过于复杂。

#### 底部时间轴 / 帧列表

底部用于管理动作和帧：

- 当前动作高亮。
- 当前帧高亮。
- 帧缩略图。
- 新增帧。
- 复制帧。
- 删除帧。
- 播放 / 暂停。
- 帧时长。

V1 只做逐帧动画，不做多轨时间轴、骨骼动画或复杂关键帧。

### 5.3 项目存档页面结构

项目存档参考截图 2，采用：

```text
顶部标题与筛选操作
项目卡片网格
```

#### 顶部标题与操作

顶部需要包含：

- 页面标题：项目存档。
- 页码或数量。
- 搜索。
- 筛选：全部、收藏、草稿、已完成、生成中、最近编辑。
- 新建项目。
- 刷新。

第一版筛选保持轻量，不做复杂多条件筛选。

#### 项目卡片网格

每张卡片代表一个像素精灵项目。

卡片需要包含：

- 精灵图或动画首帧缩略图，作为卡片主体。
- 项目名称。
- 标签，例如角色、walk、64x64、12 帧。
- 状态，例如草稿、已完成、生成中、失败。
- 最近编辑时间。
- 收藏按钮。
- 主要操作：打开编辑。
- 次要操作：复制、导出、删除，可以放在更多菜单中。

卡片状态需要明确：

- 生成中：显示进度或加载态。
- 失败：显示失败状态和重试入口。
- 空项目：显示空状态和创建入口。

### 5.4 不照搬的地方

- 不照搬复杂专业软件的全部面板，V1 默认只暴露最核心操作。
- 不让紫色和发光效果过度堆叠，避免影响长时间编辑。
- 不把所有功能都做成只有图标的重要按钮。
- 不让项目存档只变成漂亮卡片，它必须支持查找、识别状态和继续编辑。
- 不把右侧控制面板做成复杂参数中心，AI 功能应以“智能行动”形式出现。

## 6. V1 非目标

以下内容不作为 V1 必须完成范围：

- 完整复刻 Aseprite。
- 多图层专业编辑。
- 专业级多轨动画系统。
- 完整 AI 训练或模型管理。
- 复杂 prompt 模板系统。
- 复杂项目管理系统。
- 云端素材库。
- 多人协作。
- 商业化账号系统。
- 移动端完整适配。
- 骨骼动画。
- 自动补间。
- 专业级快捷键体系。
- HTML / CSS 导出承诺。

## 7. V1 验收标准

V1 是否成立，优先看是否完成以下闭环：

1. 用户可以通过 AI 生成、导入现成精灵图、导入普通图片创建精灵图项目。
2. 系统生成一个可编辑的 `SpriteDocument`。
3. 用户可以选择 `16x16`、`32x32`、`64x64` 三种画布尺寸。
4. 普通图片可以被像素化，并按像素块放进画布。
5. 现成精灵图可以被转成可编辑像素块，而不是只作为图片预览。
6. 用户可以在画布上逐像素块修改内容。
7. 用户可以在多个动作之间切换，并编辑每个动作最多 12 帧。
8. 用户可以导出当前帧 PNG。
9. 用户可以导出 sprite sheet PNG。
10. 用户可以导出 JSON。
11. 导出的结果能反映用户编辑后的内容。
12. 重新加载 JSON 后，能还原编辑结果。
13. 整体体验像一个轻量精灵图制作工具，而不是单纯的图片生成页面。
14. 产品至少包含编辑器和项目存档两个主页面。
15. 编辑器页面符合顶部状态栏、左工具栏、中央画布、右控制面板、底部帧列表的结构。
16. 项目存档页面能通过卡片网格展示项目缩略图、名称、状态、标签、时间和打开编辑入口。

## 8. 主要风险

### 8.1 AI 生成图不是真正干净的像素图

AI 结果可能有渐变、抗锯齿、杂色。V1 需要把 AI 结果当成原始素材，统一转换为用户选择的目标尺寸像素数据，而不是直接把图片当最终作品。

### 8.2 动作帧一致性

如果后续生成多帧动作，角色脸、服装、轮廓可能在不同帧里变化。V1 支持多个动作和每个动作最多 12 帧，但 AI 生成动作先做占位，不把真实 AI 多帧一致性作为第一轮阻塞项。

### 8.3 图层自动拆分

AI 图通常是一整张图片，很难可靠拆成身体、头发、武器、阴影等图层。V1 不承诺自动分层。

### 8.4 数据和导出不一致

验收重点必须放在 `SpriteDocument`、Canvas 显示、PNG 导出、sprite sheet 导出的一致性上。

## 9. 后续可扩展方向

V1 之后可以考虑扩展：

- 更多尺寸：`128x128` 和自定义尺寸。
- 更完整的动作模板和动作管理。
- 洋葱皮预览。
- 更完整的动画播放预览。
- 调色板系统。
- 图层系统。
- 素材库。
- prompt 模板。
- AI 生成多帧一致性。
- 一键生成角色完整动作集。
- 导出到游戏引擎可用格式。
