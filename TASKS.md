# 当前阶段：像素编辑与帧动画（2026-07-28）

## 已确认范围

- 核心是单张像素素材的编辑与帧动画，不再把 AI 整图生图作为用户功能。
- 工作区负责素材和布局管理；像素与帧编辑回到独立的 `SpriteDocument`。

## 本轮完成

- [x] 工作区保存改为“布局与素材引用”，像素文档独立存储，避免完整素材重复写入。
- [x] 刷新工作区时恢复最近保存的工作区。
- [x] 更新产品规格，标记旧 AI 生图方向为历史范围。

## 接下来

- [ ] 明确单图编辑页进入和返回工作区的路径，让工作区与编辑器职责更清楚。
- [ ] 完成帧复制、播放、调速、保存重开和 sprite sheet 导出的完整验收。
- [ ] 在 128×128、256×256 素材上验证编辑、缩放和网格性能。

---

# 历史：像素图生成器任务记录

## 当前阶段

- 分支：`feature/pixel-generation-workflow`
- 基线提交：`228cd60 chore: establish project baseline`
- 阶段目标：把产品从“PNG 导入 / 像素编辑器雏形”推进到“具备文字生图工作流入口”。

## 本轮进行中

- [x] 初始化 Git 仓库，建立 `main` 基线。
- [x] 创建大改分支 `feature/pixel-generation-workflow`。
- [x] 在项目页增加文字生成像素图入口。
- [x] 支持提示词、尺寸、风格选择。
- [x] 生成 4 个本地候选，并允许选择一个进入编辑器。
- [x] 编辑器显示生成来源，替换空泛的 AI 占位说明。
- [x] 增加应用图标，避免浏览器请求 favicon 时出现 404。
- [x] 补齐 PNG 导出设置：原始尺寸 / 512 预览大图。
- [x] 补齐 PNG 导出背景设置：透明背景 / 当前颜色背景。
- [x] PNG 导入支持调色板数量控制：8 / 16 / 32 / 64 / 128 / 256 / 512 色。
- [x] PNG 导入时把像素颜色归并到主色调色板，减少碎色。
- [x] 生成候选支持收藏，并在页面内显示收藏夹。
- [x] 生成历史支持恢复上一轮提示词、尺寸、风格和候选。
- [x] 生成历史和收藏候选保存到本机浏览器，刷新后可恢复。
- [x] 同级选项默认只显示当前选项，点击箭头展开更多选项。
- [x] 同级选项展开方式修正为 PC 悬浮下拉，不再把页面内容往下推。
- [x] 以用户视角跑通生成、收藏、编辑、导出、返回存档主流程。
- [x] 以移动端宽度检查项目页，没有横向溢出。
- [x] 使用 `taste-skill` 口径完成高标准 UI / 交互走查，并把问题反馈记录给经理和研发。
- [x] 按 `REVIEW.md` 的 P0 优先级优化项目页信息架构：文字生图上移为首屏主任务，新建空白和 PNG 导入降级为辅助入口。
- [x] 修复生成风格下拉：支持 Esc 关闭，展开浮层不再遮挡“生成 4 个候选”，点击生成前自动收起。
- [x] 优化编辑器默认视野：画布按当前视口 fit-to-view，进入编辑器首屏能完整看到 64x64 图。
- [x] 下沉开发态信息：JSON 导出放入“更多”，右侧面板优先展示导出设置和图层。
- [x] 复测移动端项目页和编辑器：`390x844` 下无横向溢出，项目页首屏可见生成入口，编辑器可完整看到画布。
- [x] 把“文字生成像素图”和“已有素材编辑”调整为桌面端左右布局，移动端保持上下堆叠。
- [x] 生成候选卡片增加删除按钮，删除后同步更新当前候选、生成历史和收藏状态。
- [x] 固定生成候选卡片宽度，桌面端一行最多 4 个，候选少时不再被拉宽顶满。

## 下一步建议

- 下一阶段主线调整为“轻量 Figma 式像素无限画布工作区”。
- 旧的 `REVIEW.md` P1 问题暂缓，避免和新画布主线抢范围。
- 接真实 AI 生成接口暂缓，先把导入图片后的像素化质量和多对象画布做好。
- 移动端第一阶段只保证查看、选中、调整参数和导出，不追求完整 Figma 多图排版体验。

## 下一阶段待办：像素无限画布工作区

- [ ] 扩展像素尺寸类型：从 `16 / 32 / 64` 扩到 `16 / 32 / 64 / 128 / 256`。
- [ ] 统一图片导入边界：从偏 PNG 导入扩展到 PNG / JPG / WebP，并保留透明背景处理。
- [ ] 设计并实现 Workspace 数据结构：保存画布项目、视口、对象列表、选中对象。
- [ ] 设计并实现 CanvasItem 数据结构：每张图片对象保存位置、显示大小、像素分辨率、调色板数量、原图来源和内部 `SpriteDocument`。
- [ ] 新增 Workspace 本机保存逻辑，刷新后可恢复画布对象和像素化设置。
- [ ] 新增无限画布基础 UI：平移、缩放、选择对象、移动对象。
- [ ] 支持导入多张图片并自动放入画布中心。
- [ ] 右侧面板显示当前选中对象的分辨率、调色板数量、导出设置和预览。
- [ ] 支持单对象重新像素化，默认偏游戏像素画感。
- [ ] 支持单对象 PNG 导出，不包含棋盘格、画布背景或其他对象。
- [ ] 高分辨率对象如 `256x256` 默认关闭密集网格，避免视觉噪音和性能问题。
- [ ] 验证桌面端主流程：创建工作区、导入多图、移动、改单图分辨率、保存、刷新恢复、单图导出。
- [ ] 验证移动端基础流程：查看、选中对象、调整参数、导出。
