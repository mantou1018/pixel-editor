# 像素图生成器决策记录

## 2026-07-28：以单图编辑和帧动画为核心，AI 整图生成退出当前范围

- 决策：当前产品的主任务是编辑已有像素素材、制作和导出帧动画；面向用户的文字生图和 AI 整图候选不再作为当前功能方向。
- 原因：用户已经明确需要的是可靠的像素编辑体验与动画制作。继续让 AI 生图占据产品入口，会分散编辑器、保存和动画闭环的开发与体验重点。
- 影响：若以后接入 AI，只考虑以当前静态素材为输入，生成可逐帧接受和修正的动作序列；不会回到“文字直接生成一张像素图”的路径。

## 2026-07-28：工作区只保存布局引用，`SpriteDocument` 单独保存

- 决策：工作区对象保存布局和 `spriteId`，完整 `SpriteDocument`（包含像素、帧和原图来源）独立写入本机存储。
- 原因：把完整素材嵌进每个工作区对象，会在保存、复制和恢复时重复序列化大块像素数据，是高分辨率编辑变卡和数据难维护的结构性风险。
- 影响：刷新时按引用重新组合工作区和素材；已有旧工作区继续兼容读取，并在下次保存时迁移。工作区删除会删除其引用的独立素材，当前不支持跨工作区共享同一素材。

---

## 2026-06-09：先做生图工作流外壳，不直接接真实 AI

- 决策：本轮先实现本地“文字生成像素图”工作流，提供提示词、风格、尺寸、4 个候选和进入编辑器能力。
- 原因：当前项目还没有稳定的 AI 接口、密钥、计费和失败处理边界。先把产品路径跑通，能降低后续接真实 AI 时的返工风险。
- 影响：当前候选图是本地算法生成的可编辑像素数据，不代表真实 AI 质量；但它已经使用 `SpriteDocument` 数据结构，后续可以替换生成来源。
- 暂不做：真实 AI 调用、云端存储、账号系统、批量生成、局部重绘。

## 2026-06-09：为大改建立 Git 基线和功能分支

- 决策：在项目目录初始化 Git，`main` 保存当前基线，后续改动放在 `feature/pixel-generation-workflow` 分支。
- 原因：本轮属于较大改动，需要明确回退点和版本边界。
- 影响：项目新增 `.git/` 和 `.gitignore`；`node_modules/`、`.next/` 不纳入版本控制。

## 2026-06-10：PNG 导出必须区分原始素材和展示预览

- 决策：编辑器导出 PNG 时提供“原始尺寸”和“预览大图”两个选项。
- 原因：像素素材交付需要真正的 `16x16`、`32x32` 或 `64x64` 文件；展示、分享和检查细节时又需要放大图。
- 影响：默认导出原始尺寸透明 PNG；用户可切换到最长边约 `512px` 的预览大图。
- 相关背景：此前用户发现 64x64 结果实际导出为 512x512 且文件偏大，因此导出语义需要在 UI 上明确。

## 2026-06-10：PNG 背景必须由导出设置显式控制

- 决策：导出背景提供“透明背景”和“当前颜色背景”两个选项。
- 原因：游戏素材通常需要透明背景，但用户也会需要带底色的展示图。不能把编辑器里的棋盘格或预览背景写进 PNG。
- 影响：透明背景保留透明像素；当前颜色背景会在导出时先铺底色，再绘制可见图层。

## 2026-06-10：PNG 导入先做调色板归并，不先做复杂像素艺术滤镜

- 决策：PNG 导入增加 8 / 16 / 32 / 64 / 128 / 256 / 512 色调色板数量选择，并把每个像素归并到最近的主色。
- 原因：用户当前最直接的问题是像素化结果颜色碎、不够像像素图；调色板归并是低风险且可控的第一步。
- 影响：导入普通图片时颜色数量可控，后续可以继续叠加轮廓增强、去背景强度和抖动策略。
- 暂不做：自动轮廓增强、复杂聚类算法、手动调色板编辑、局部预览对比。

## 2026-06-10：PNG 导入最高调色板数量放宽到 512 色

- 决策：导入调色板上限从 64 色提高到 512 色，并在项目页提供 128 / 256 / 512 色按钮。
- 原因：64 色对普通图片或细节较多的素材太少，会过度合并颜色；512 色能保留更多原图层次。
- 影响：64x64 图最多 4096 个像素，512 色匹配量仍可接受；低色数选项继续保留，方便做更像传统像素画的强约束效果。

## 2026-06-10：同级选项默认折叠

- 决策：项目页上的尺寸、PNG 导入颜色数、生成风格都改为“当前选项 + 箭头展开更多选项”。
- 原因：并列按钮过多会让页面显得拥挤，尤其是颜色数增加到 512 后，用户第一眼不需要同时看到全部选项。
- 影响：默认状态更精简；用户仍可通过箭头展开完整选项，选择后自动收起。

## 2026-06-11：同级选项展开必须是 PC 浮层下拉

- 决策：尺寸、PNG 导入颜色数、生成风格的更多选项使用悬浮下拉弹窗，不占用文档流。
- 原因：PC 端下拉控件不应该把后续内容整体顶开；行内展开会造成布局跳动，交互观感不专业。
- 影响：展开选项时按钮和下方内容位置保持不变，选项浮在控件下方，选择后收起。

## 2026-06-10：生成迭代先做页面内历史和收藏，不先做持久化

- 决策：项目页先支持页面内生成历史和收藏候选，用户可以回看本轮生成、收藏候选、从收藏进入编辑器。
- 原因：用户对交互不满意，当前最明显缺口是“生成后没有可回看和取舍的过程”。页面内状态能先验证交互路径，不急着引入新的持久化结构。
- 影响：刷新页面或离开项目页后，生成历史和收藏会消失；真正保存下来的仍是“选中并编辑”后创建的项目。
- 后续：如果这个交互方向确认可用，再把生成历史存入 IndexedDB。

## 2026-06-10：生成工作台状态改为本机浏览器持久化

- 决策：生成历史和收藏候选改存入 IndexedDB 的 `generation_workspace`，页面刷新后恢复最近一轮候选、提示词、尺寸、风格和收藏。
- 原因：测试从用户视角发现“生成后收藏了，但刷新就丢”会破坏取舍和迭代感；对像素生图软件来说，用户需要能回到最近一次生成现场。
- 影响：历史和收藏会保存在当前浏览器本机，不是云端账号同步；换设备、清浏览器数据或使用新的无痕上下文仍会丢失。
- 取舍：仍只保留最近 6 轮历史，避免本地存储无限膨胀；真实项目存档仍通过“选中并编辑 / 编辑此收藏”创建。

## 2026-07-05：下一阶段改为轻量 Figma 式像素无限画布

- 决策：下一阶段做可保存的轻量 Figma 式像素无限画布，第一阶段只取“无限画布 + 多张图片自由摆放”的核心能力。
- 用户确认：对“像 Figma”的理解优先是无限画布和多对象摆放，不要求第一版完整复刻图层、编组、对齐线、快捷键和多人协作。
- 原因：用户的核心诉求是画布、图片像素化、每张图独立调分辨率和导出。完整 Figma 能力会显著扩大范围，反而拖慢核心闭环。
- 影响：实现上新增工作区和画布对象层；现有单张 `SpriteDocument`、PNG 导入、像素化、导出能力继续复用。
- 暂不做：旋转、编组、评论、多人协作、完整设计工具能力、整张画布导出。

## 2026-07-05：主要用途优先独立小像素图，不优先拼大图

- 决策：下一阶段默认围绕“很多独立小像素图”设计，单对象导出优先于整张画布导出。
- 用户确认：主要用途选 A，即制作头像、图标、游戏素材等独立小图，而不是把多张图拼成一张大图。
- 原因：独立小图场景下，每张图的像素分辨率、像素化参数和导出边界都应独立保存。
- 影响：画布对象之间不共享像素数据；每个对象单独保存原图来源、分辨率、调色板数量和像素数据。
- 后续：如果未来要做拼大图，再补整画布导出、对齐工具和画布级尺寸设置。

## 2026-07-05：默认像素化效果偏游戏像素画感

- 决策：图片自动像素化的默认目标不是最大程度保留照片细节，而是偏游戏像素画感：颜色更少、更干净、边缘更清晰。
- 用户确认：像素化效果选择 B，即更像游戏像素画，颜色更少、更干净。
- 原因：普通图片直接缩小容易出现碎色、脏边和模糊感；像素画工具需要主动控制颜色数量和缩放方式。
- 影响：默认参数应偏低色数和清晰像素显示；仍可提供调色板数量控制，让用户在干净和细节之间调整。
- 质量边界：预览和导出不能使用模糊缩放；透明背景棋盘格只用于预览，不能进入导出 PNG。

## 2026-07-05：无限画布项目必须可保存和恢复

- 决策：无限画布不是临时转换页，而是可保存的工作区项目。
- 用户确认：需要像 Figma 文件一样保存工作区，下次继续打开。
- 原因：多张图、多对象位置、每张图的像素化参数都需要持续存在，否则工作区价值会很弱。
- 影响：需要新增 Workspace 数据结构和本机保存逻辑，保存对象列表、位置、分辨率、像素化参数、原图来源和选中状态等信息。
- 取舍：第一阶段仍保存到本机浏览器，不做云端账号同步。
